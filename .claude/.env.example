# AI 로그 수집 설정 (gitignore됨 — 커밋 금지)
# Supabase 프로젝트 판 뒤 채우기. 비어있으면 로컬 jsonl만 적재됨.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# 선택: 비우면 자동 추론 (git config / 기본값)
PROJECT=
# USER_NAME=

# 어떤 CLI 도구로 작업했나: claude | codex
TOOL=

# 서브에이전트 역할(planner/reviewer 등). 지금은 없음 → 비워두면 null.
# AGENT_ROLE=
