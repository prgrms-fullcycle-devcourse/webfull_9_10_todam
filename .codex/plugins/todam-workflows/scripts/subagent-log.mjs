// #!/usr/bin/env node
// // Plugin wrapper. Run from the Todam repo root.
// import { spawnSync } from "node:child_process";

// const result = spawnSync(process.execPath, [".codex/scripts/subagent-log.mjs", ...process.argv.slice(2)], {
//   stdio: "inherit",
// });

// process.exit(result.status ?? 0);

