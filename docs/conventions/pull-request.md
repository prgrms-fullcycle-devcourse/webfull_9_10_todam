# Pull Request Rules

이 문서는 PR 양식 자체가 아니라, PR 본문에 어떤 정보가 담겨야 하는지 정의한다.
실제 작성 시점에는 `.github/PULL_REQUEST_TEMPLATE.md`의 섹션을 사용하되, 아래 기준에 맞게 내용을 채운다.

## Create Criteria

- 기능 단위 작업 완료 시 PR을 생성한다.
- 기본 대상 브랜치는 `dev`이다.

## Title Format

PR 제목은 아래 형식을 따른다.

```text
[type] #이슈번호 제목
```

예시:

```text
[feat] #10 예약 생성 API 구현
```

## Body Rules

- PR 본문은 한국어로 작성한다.
- 각 섹션은 짧은 불릿 중심으로 작성한다.
- 관련 이슈에는 PR이 닫거나 연결하는 이슈 번호를 적는다.
- 작업 내용은 구현 사실만 간결하게 적는다.
- 테스트 결과는 실제로 확인한 항목만 적는다.
- 리뷰 포인트는 리뷰어가 판단해야 할 질문이나 주의해서 볼 변경점을 적는다.
- 체크리스트는 PR 작성 시점 기준으로 사실대로 표시한다.

## Body Tone Example

아래 예시는 권장 톤과 밀도를 보여주기 위한 참고 예시다.

```md
## 📌 관련 이슈

- close #123

## ✨ 작업 내용

- Reservation 엔티티와 Participant 엔티티 간 1:N 관계를 설정했습니다.
- 예약 생성 시 고유한 6자리 URL slug를 생성하도록 구현했습니다.
- NestJS `ValidationPipe`를 적용해 입력값 검증을 추가했습니다.

## 📸 스크린샷 / 테스트 결과

- `POST /reservations` 성공 응답을 확인했습니다.

## 🔍 리뷰 포인트

- 6자리 slug 생성 방식이 중복을 충분히 방지하는지 봐주시면 좋겠습니다.
- 예약 생성 과정에서 Prisma 트랜잭션 경계가 적절한지 확인 부탁드립니다.
```

## Required Context

- PR 생성 직전에 현재 diff, 커밋 로그, 테스트 결과를 기준으로 본문을 작성한다.
- 커밋 메시지나 PR 본문 작성 전에는 변경 파일별 요약을 먼저 정리한다.
- 파일별 요약은 `파일 경로: 변경 이유 또는 핵심 수정 내용` 형식으로 정리한다.
- 이슈 번호, PR 제목, 확인한 테스트 결과가 있으면 PR 본문에 반영한다.
