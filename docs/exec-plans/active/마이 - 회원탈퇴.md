# Feature Plan: 마이 - 회원탈퇴

## Summary

- Goal: 인증된 User가 본인 재확인(이메일: 비밀번호 / 소셜: OAuth 토큰)을 거쳐 계정을 탈퇴 처리한다. 탈퇴 가능 조건(진행 중 예약·작품 없음, 파트너 해지 선행) 검증 → 개인정보 익명화 → 세션 무효화 → 탈퇴 완료 페이지 이동의 전체 플로우를 구현한다.
- Owner: (BE 미정 / FE 미정)
- Date: 2026-06-05

## Status

<!--
게이트가 읽는 체크리스트. 셋 다 [x] 여야 completed/ 이동 가능 (pre-commit이 강제).
- API 구현: 실 BE(`apps/api`) 엔드포인트가 contract대로 존재·동작. MSW mock만 있으면 미체크.
- UI 구현: 화면/컴포넌트 구현 완료.
- API 연동: **실 API** 요청/응답이 contract 스키마로 연결. MSW mock 바인딩만 한 상태는 미체크(연동 아님).
-->

- [x] API 구현
- [ ] UI 구현
- [ ] API 연동

> API 구현: 2026-06-09 `DELETE /users/me` BE 구현 완료(drift 0, reviewer 통과, jest 196). UI/연동(진입링크·모달·useWithdraw·MSW)은 FE 레인 + `ProfileEditScreen` #185 의존 → 핸드오프. 3항목 모두 [x] 전까지 completed/ 미이동.

## Reconcile (2026-06-09) — 코드 기준 정합 (plan 2026-06-05 → stale)

원 plan 작성 후 **user 모듈이 구축됨**(마이페이지 프로필 GET/PATCH, PR #241 머지). 아래가 implementer 바인딩 SSOT(원 API Contract 섹션의 `name/phone` 익명화·`providerUserId`·`PENDING/CONFIRMED` 차단 등은 **본 블록이 보정**).

**코드 검증 결과(2026-06-09)**:

- **BE user 모듈 존재** (`apps/api/src/modules/user/`): `user.controller.ts`(GET/PATCH `users/me`), `application/use-cases/{get-my-profile,update-my-profile}`, `domain/repositories/user.repository.ts`(`findById`/`existsByNicknameExceptUser`/`updateNickname`), `infrastructure/persistence/prisma-user.repository.ts`, `user.module.ts`. → **withdraw 는 이 모듈에 ADD**(scratch 생성 아님). `@Delete('users/me')` 라우트를 기존 `user.controller.ts` 에 추가.
- **User 스키마 (실제)**: `id, nickname(VarChar100), email(unique), password(String? — 소셜 가입자는 null), isPartner(bool), emailVerified, status(UserStatus), createdAt, updatedAt, withdrawnAt`. **`name`·`phone` 컬럼 없음.** 관계: `oauthAccounts`, `partner Partner?`, `refreshTokens`, `reservations`, `reviews`, `notificationSettings`, `notifications`, `artworkLogs`, `reports`.
  - **익명화 대상 = `email`, `nickname`** (name/phone 아님). `password`도 `null` 처리. 포맷: `withdrawn_<sha256(id+withdrawnAt)>` (email 은 unique 제약 충돌 회피 위해 해시 + `@anonymized` 등 도메인 형식 — 구현 시 unique 보장).
- **본인 재확인**: `bcrypt.compare(dto.password, user.password)` — auth `login.use-case.ts:26` 동일 패턴(`bcrypt`). 소셜 유저(`password === null`)는 password 분기 불가 → D-2.
- **OAuthAccount**: `{ id, userId, provider(OAuthProvider), providerId(VarChar255) }` (plan 의 `providerUserId` → 실제 `providerId`). 탈퇴 시 userId 연동 전면 삭제. unlink 외부 호출은 D-2(Out 가능).
- **RefreshToken**: `{ id, userId, tokenHash, expiresAt, createdAt }` (plan 의 `token` → 실제 `tokenHash`). 탈퇴 시 userId 전체 삭제.
- **Partner**: `{ userId(unique), status(PartnerStatus), terminatedAt, approvedAt, ... }`. `User.isPartner` bool 도 존재. → 파트너 보유 차단은 D-3.
- **ReservationStatus** (8종): `PENDING, CONFIRMED, CANCELED, IN_PROGRESS, SHIPPED, DELIVERED, PICKUP_READY, PICKUP_DONE`. **차단 집합 재정의 필요**(원 plan 의 `PENDING/CONFIRMED` 만으로는 IN_PROGRESS/SHIPPED/PICKUP_READY 진행 건을 놓침) → D-6.
- **ArtworkStatus** (8종): `RESERVED, VISITED, DRYING, BISQUE_FIRING, GLAZING, GLAZE_FIRING, COMPLETED, CANCELED`. 종료 = `COMPLETED, CANCELED`. 그 외 제작 진행 → 차단 후보 → D-6.

**결정 확정 (2026-06-09, 사용자)**:

- **D-6 (차단 상태 집합) — RESOLVED: 종료상태 제외 전부 차단.**
  - 예약 차단 = `status NOT IN (CANCELED, DELIVERED, PICKUP_DONE)` → `PENDING/CONFIRMED/IN_PROGRESS/SHIPPED/PICKUP_READY` 중 1건이라도 있으면 차단.
  - 작품 차단 = `status NOT IN (COMPLETED, CANCELED)` → 그 외 제작 진행 작품 1건이라도 있으면 차단.
  - 둘 중 하나라도 걸리면 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST`.
- **D-2 (소셜 유저 본인확인) — RESOLVED: 소셜은 의사확인만.**
  - 이메일 유저(`password != null`): body `password` 를 `bcrypt.compare(password, user.password)` 검증, 불일치 400 `PASSWORD_MISMATCH`. body `password` 누락 시 400(검증).
  - 소셜 유저(`password == null`): 본인 재확인 **생략**(모달 의사확인만, body 없이/무시 허용). oAuthAccessToken 외부검증·OAuth unlink 외부호출은 **Out**(후순위).
- **D-3 (파트너 보유 차단) — RESOLVED: 차단(신규 코드).**
  - `Partner` 레코드가 `status = APPROVED` 이고 `terminatedAt = null` 이면 400 **`ACTIVE_PARTNER_EXISTS`** ("파트너 해지 후 탈퇴가 가능합니다."). 체크 **순서 = 파트너 → 예약/작품 → 본인확인**.
- D-5 (5년 DELETED 스케줄러) = **Out** 확정. `withdrawnAt` 기록만.

**확정 BE 처리 순서 (DELETE /users/me)**:
1. AuthGuard → `userId`. 미인증 401 `UNAUTHORIZED`.
2. **파트너 차단**: APPROVED & 미해지 Partner 보유 시 400 `ACTIVE_PARTNER_EXISTS`.
3. **진행 건 차단**: 예약(비종료) 또는 작품(비종료) 존재 시 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST`.
4. **본인확인**: 이메일 유저 password bcrypt 검증(불일치 400 `PASSWORD_MISMATCH`), 소셜 유저 생략.
5. **트랜잭션**: `users` → `status=WITHDRAWN, withdrawnAt=now(), email/nickname 익명화(unique 보장 해시), password=null` / `oauth_accounts` userId 삭제 / `refresh_tokens` userId 삭제.
6. 응답 헤더 `Set-Cookie: refresh_token=; Max-Age=0; HttpOnly; Secure` 쿠키 만료.
7. 200, `data: null`, `@ResponseMessage('회원 탈퇴가 정상적으로 처리되었습니다.')`.

**이번 turn 범위 = BE `DELETE /users/me` 만.** FE(진입 링크·확인 모달·useWithdraw·MSW)는 FE 레인 + `ProfileEditScreen`(#185 미구현) 의존 → **핸드오프**. shared `withdrawBodySchema`(`{ password?: string }` — 소셜 대비 optional)만 additive 추가.

## Context

- 요구사항명세서(고정): `docs/requirements.md`
  - `인증 auth` §6 회원 탈퇴 — 탈퇴 가능 조건, 본인 재확인, 익명화, 세션 무효화, 30일 재가입 제한, 5년 보관 전체 절차.
  - `파트너 partner` §1 파트너 상태 — 탈퇴 시 `APPROVED` Partner capability 보유 시 파트너 해지 선행 필요.
  - `접근 주체` — `User` 이상(`@UseGuards(AuthGuard)`). 미인증 401.
- 기능명세 (Notion 기능명세 DB `b242ee66b06c8349805601ce4a05247a` — `--find "회원탈퇴"` select):
  - "회원탈퇴" — 실행주체: user/partner, 도메인: user. 선행조건: 인증된 사용자·본인 확인 완료. 동작: 계정 삭제 요청, 인증 정보 제거, 로그인 해제. 비고: 비밀번호 확인 기능 기대.
- API명세 (Notion API명세 DB `5852ee66b06c838bb8ec01c6bf4f2e25` — `--find "users/me"` select로 확인):
  - `DELETE /users/me` — 회원 탈퇴 (정본)
- 선행 plan(패턴 재사용):
  - `docs/exec-plans/active/마이페이지.md` — `/users/me` contract 봉투 패턴, AuthGuard, apiFetch, MSW 경로 규칙.
  - `docs/exec-plans/active/로그아웃.md` — 세션 무효화(refreshToken 쿠키 만료, localStorage `accessToken` 제거, router.push) 패턴.
- Open decisions:
  - ~~D-1. 탈퇴 진입점 위치~~ — RESOLVED (Figma: 개인 정보 수정 화면 푸터 "탈퇴하기" 링크)
  - D-2. 소셜 계정 탈퇴 시 oAuthAccessToken 수집 방법
  - D-3. 파트너 capability 보유 유저의 BE 선행 검증 방식
  - ~~D-4. 탈퇴 완료 페이지(라우트) 확정~~ — RESOLVED (별도 페이지 없음, 메인 `/` 이동)
  - D-5. 5년 후 DELETED 전이 스케줄러 — MVP 포함 여부

## API Contract (스냅샷)

<!-- Notion API명세 `DELETE /users/me` 기준 고정. BE/FE/reviewer SSOT. -->

### 데이터모델

| 모델 | 필드 | 비고 |
|---|---|---|
| `User` (`users`) | `id`, `email`, `name`, `phone`, `status`(UserStatus), `withdrawnAt`, `updatedAt` | 탈퇴 시 `status = 'WITHDRAWN'`, `withdrawn_at = now()`, email/name/phone → 해시 익명화 |
| `OAuthAccount` (`oauth_accounts`) | `userId`, `provider`, `providerUserId` | 탈퇴 시 전면 삭제 |
| `RefreshToken` (`refresh_tokens`) | `userId`, `token`, `expiresAt` | 탈퇴 시 해당 유저 전체 삭제 |

`enum UserStatus`: `ACTIVE`, `WITHDRAWN`, (`DELETED`) — 5년 후 자동 전이.

### 엔드포인트

#### `DELETE /users/me` — 회원 탈퇴

**인증:** `Authorization: Bearer {accessToken}` (AuthGuard, payload `{ sub: userId }`)

**Headers:**
```
Content-Type: application/json
Accept: application/json
Authorization: Bearer {accessToken}
```

**Request Body (이메일 가입 유저):**
```json
{
  "password": "Password1234!"
}
```

**Request Body (소셜 가입 유저):**
```json
{
  "oAuthAccessToken": "social_access_token_for_unlink_verification"
}
```

**시스템 처리:**
1. AuthGuard로 accessToken 검증, `userId` 추출. 미인증 401.
2. **탈퇴 불가 조건 검증:**
   - `reservations` 에서 해당 userId의 `status IN ('PENDING', 'CONFIRMED')` 건 존재 시 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST`.
   - `artworks` 에서 해당 userId와 연동된 작품 중 최종 완료/종료(`COMPLETED`, `CANCELED`, `DELIVERED`, `PICKUP_DONE` 제외) 진행 중 작품 존재 시 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST`.
   - (요구사항 추가) `partner` 에서 `status = 'APPROVED'` 보유 시 파트너 해지 선행 필요 → 별도 에러 코드 미정(D-3).
3. **본인 재확인:**
   - 이메일 유저: `password` 해시화 후 DB 비교 → 불일치 시 400 `PASSWORD_MISMATCH`.
   - 소셜 유저: `oAuthAccessToken`으로 외부 인증기관 검증 → 실패 시 400 `PASSWORD_MISMATCH`.
4. `users` 테이블: `status = 'WITHDRAWN'`, `withdrawn_at = now()`, 개인정보(email/name/phone) → 해시 익명화.
5. `oauth_accounts` 에서 userId 연동 레코드 전면 삭제. (카카오·Google unlink API 호출 — D-2와 연계)
6. `refresh_tokens` 에서 userId 전체 삭제.
7. 응답 헤더에 `Set-Cookie: refresh_token=; Max-Age=0; HttpOnly; Secure` 로 쿠키 만료.
8. 200 반환.

**200 OK (공통 봉투 `data` 본문):**
```json
{
  "statusCode": 200,
  "timestamp": "2026-05-25T16:40:00.115Z",
  "path": "/users/me",
  "message": "회원 탈퇴가 정상적으로 처리되었습니다.",
  "data": null,
  "error": null
}
```
> FE는 200 수신 후 localStorage `accessToken` 제거 → 메인화면(`/`) 이동 [D-4 확정].

**에러:**

| status | error code | message |
|---|---|---|
| 400 | `PASSWORD_MISMATCH` | "본인 확인을 위한 인증 정보가 일치하지 않습니다." |
| 400 | `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST` | "진행 중인 클래스 예약 또는 제작 중인 도자기 작품이 남아있어 탈퇴가 불가능합니다." |
| 401 | `UNAUTHORIZED` | "인증 정보가 유효하지 않거나 만료되었습니다." |
| 500 | `INTERNAL_SERVER_ERROR` | "회원 탈퇴 처리 중 서버 내부 오류가 발생했습니다." |

> 요구사항에 정의된 에러(`진행 중 Partner capability 존재`) 코드는 API명세 미정 → D-3.

### BE 구현 현황 (코드 직접 확인)

`apps/api/src/modules/user/` 디렉터리는 존재하나 **모든 파일이 `.gitkeep` 빈 stub** — controller, use-case, repository 전무. `apps/api/src/modules/auth/` 에도 withdraw/탈퇴 관련 use-case 없음(`logout.use-case.ts`만 존재). **`DELETE /users/me` 미구현.**

`apps/web/src/mocks/handlers.ts` 에도 `DELETE /users/me` MSW mock 없음. FE 진입점·화면 없음.

## Scope

- In:
  - **BE:** `apps/api` `user` 모듈에 `DELETE /users/me`(AuthGuard). 탈퇴 불가 조건 검증, 본인 재확인(이메일/소셜), 익명화, oauth_accounts 삭제, refresh_tokens 삭제, 쿠키 만료 처리.
  - **FE 진입점 [D-1 확정]:** 개인 정보 수정 화면(`/my/profile` = `ProfileEditScreen`) 하단 푸터에 "탈퇴하기" 링크 추가. 안내문 "혹시 리듬을 떠나고 싶으신가요?" + 링크, 둘 다 `foreground-tertiary`.
  - **FE 탈퇴 확인 모달 [Figma 8505:19302/19303]:** `@todam/ui` `Modal` `type="shortText"` `danger={true}` 재사용. title "정말 탈퇴하시겠어요?", description "지금까지 기록한 소중한 작품들이 모두 사라져요.", cancelLabel "다음에 하기", confirmLabel "탈퇴하기"(빨강 `#DC2B26`). `useModal` 스토어로 오픈(로그아웃 모달 패턴 동일).
  - **FE 본인 재확인:** 이메일 유저 비밀번호 입력 / 소셜 유저 재인증 안내(D-2). 확인 모달의 confirm → 재확인 입력 → `DELETE /users/me` 호출 → 성공 시 accessToken 제거 + 탈퇴 완료 페이지 이동.
  - **FE 탈퇴 완료 처리 [D-4 확정]:** 200 성공 → accessToken 제거 → 메인화면(`/`) `router.push('/')` + 완료 토스트. 별도 완료 페이지 없음.
  - **MSW mock:** `DELETE /api/v1/users/me` 핸들러 추가 (실 API 전환 전 개발용).
  - **shared contract:** `packages/shared/src/contracts/user-me.ts` 에 `WithdrawBody` / withdraw 응답 타입 추가.

- Out:
  - 5년 후 자동 `DELETED` 전이 스케줄러 (D-5 — MVP 이후 후순위).
  - 카카오·Google OAuth unlink API 실제 호출 (D-2 — 소셜 토큰 확보 방법 미정, OAuth 미연동 시 stub 처리).
  - 30일 재가입 차단 로직 (요구사항 정의, API명세 미정 — MVP 이후).
  - 마케팅·알림 수신 동의 철회 (notification-settings 연계 — 탈퇴 시 BE에서 cascade 처리, FE 별도 UI 없음).
  - 파트너 해지 플로우 자체 (파트너 plan 소관 — 탈퇴 BE에서 사전 차단만).

## Plan

### BE

1. `apps/api/src/modules/user/` 에 `withdraw.use-case.ts` 신설.
   - 탈퇴 불가 조건 검증 (reservation repository, artwork repository 의존성 주입).
   - 이메일/소셜 분기 본인 재확인 (`authType` 판별: `password` 필드 존재 여부 또는 User.provider 조회).
   - 익명화: `email`, `name`, `phone` → `WITHDRAWN_<sha256(id+timestamp)>` 형태 치환.
   - `status = WITHDRAWN`, `withdrawnAt = now()` 저장.
   - OAuthAccount 삭제, RefreshToken 전체 삭제.
2. `apps/api/src/modules/user/presentation/controllers/` 에 `user.controller.ts` 신설.
   - `@Delete('users/me')` `@UseGuards(AuthGuard)` — `WithdrawDto` body validation.
   - `@Res({ passthrough: true })` 로 refreshToken 쿠키 만료 처리.
3. `WithdrawDto` (`password?: string`, `oAuthAccessToken?: string`) + class-validator 이중 배타 검증.
4. `user.module.ts` 구성(useCase, controller, prisma-user.repository, 의존 모듈 import).

### FE

5. `packages/shared/src/contracts/user-me.ts` 에 타입 추가:
   ```ts
   export const withdrawBodySchema = z.union([
     z.object({ password: z.string().min(1) }),
     z.object({ oAuthAccessToken: z.string().min(1) }),
   ]);
   export type WithdrawBody = z.infer<typeof withdrawBodySchema>;
   ```
6. `apps/web/src/features/user/profile/api.ts` 에 `withdrawAccount(body: WithdrawBody)` 함수 추가.
7. `apps/web/src/features/user/profile/` 에 `useWithdraw` mutation hook 추가 (성공 시 localStorage `accessToken` 제거 + `router.push('/')` 메인 이동 + 완료 토스트).
8. **탈퇴 확인 모달** [Figma 8505:19302/19303] — `@todam/ui` `Modal` `type="shortText"` `danger={true}` 재사용, `useModal`로 오픈(로그아웃 모달 패턴):
   - title "정말 탈퇴하시겠어요?", description "지금까지 기록한 소중한 작품들이 모두 사라져요.", cancelLabel "다음에 하기", confirmLabel "탈퇴하기"(빨강).
   - confirm → 본인 재확인 단계(이메일: 비밀번호 입력 / 소셜: 재인증 안내 D-2) → `DELETE /users/me`.
   - 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST` → "진행 중 예약/작품이 있어 탈퇴할 수 없습니다" 토스트.
   - 400 `PASSWORD_MISMATCH` → "비밀번호가 일치하지 않습니다" 토스트.
9. ~~탈퇴 완료 페이지 신설~~ — [D-4 확정] 별도 페이지 없음. 성공 시 메인(`/`) 이동으로 대체.
10. **진입점 추가** [D-1 확정]: `ProfileEditScreen`(개인 정보 수정 `/my/profile`) 하단 푸터에 안내문 "혹시 리듬을 떠나고 싶으신가요?" + "탈퇴하기" 링크(`foreground-tertiary`). 클릭 시 8의 확인 모달 오픈.
11. **MSW mock** `apps/web/src/mocks/handlers.ts` 에 `http.delete('/api/v1/users/me', ...)` 핸들러 추가.

## Out (단계별 완료물)

### BE 구현 완료 (2026-06-09)

- **`packages/shared/src/contracts/user-me.ts`** — `withdrawBodySchema` / `WithdrawBody` 추가 (additive)
- **`apps/api/src/modules/user/domain/repositories/user.repository.ts`** — `WithdrawTargetRow` 인터페이스 + 추상 메서드 5개 추가 (`findWithdrawTarget` / `hasActivePartner` / `hasActiveReservations` / `hasActiveArtworks` / `withdraw`)
- **`apps/api/src/modules/user/infrastructure/persistence/prisma-user.repository.ts`** — 위 5개 메서드 구현. `withdraw` 는 `$transaction`으로 users 익명화(sha256 결정적 해시) + oauth_accounts deleteMany + refresh_tokens deleteMany
- **`apps/api/src/modules/user/presentation/dto/withdraw.dto.ts`** — `WithdrawDto extends createZodDto(withdrawBodySchema)` (신규)
- **`apps/api/src/modules/user/application/use-cases/withdraw.use-case.ts`** — 처리 순서: 유저 조회·ACTIVE 가드 → 파트너 차단 → 예약/작품 차단 → 이메일 bcrypt 검증 / 소셜 생략 → withdraw 트랜잭션 (신규)
- **`apps/api/src/modules/user/application/use-cases/withdraw.use-case.spec.ts`** — 9개 단위 테스트 (이메일 정상/비번불일치/비번누락/소셜 정상/파트너400/예약400/작품400/WITHDRAWN404/없음404) 전부 통과 (신규)
- **`apps/api/src/modules/user/presentation/controllers/user.controller.ts`** — `@Delete('users/me')` 추가 (`@UseGuards(AuthGuard)`, `@HttpCode(200)`, `@ResponseMessage(...)`, `res.clearCookie('refresh_token', ...)` 로그아웃 동일 옵션)
- **`apps/api/src/modules/user/user.module.ts`** — `WithdrawUseCase` provider 등록
- **`apps/api/src/modules/api-routes.snapshot.spec.ts`** — `DELETE /users/me` 신규 라우트 스냅샷 등록

결정 반영:
- D-6: 예약 차단 = `status NOT IN (CANCELED, DELIVERED, PICKUP_DONE)`, 작품 차단 = `status NOT IN (COMPLETED, CANCELED)`
- D-2: 소셜(password=null) 본인확인 생략, 이메일 bcrypt 검증 필수
- D-3: `ACTIVE_PARTNER_EXISTS` 400 차단, 순서 = 파트너 → 예약/작품 → 본인확인
- 익명화: `sha256(userId)` 결정적 해시, email=`withdrawn_<hash16>@anonymized.todam`, nickname=`탈퇴회원_<hash8>`

### 미완료 (FE 레인)

- UI:
  - 탈퇴 확인 모달 (`@todam/ui` Modal type=shortText danger=true, `useModal` 오픈)
  - (탈퇴 완료 페이지 없음 — [D-4 확정] 성공 시 메인 `/` 이동)
  - 진입 링크 ([D-1 확정] `ProfileEditScreen` 개인 정보 수정 `/my/profile` 하단 푸터 "탈퇴하기")
- 연동:
  - MSW mock `DELETE /api/v1/users/me`
  - `apps/web/src/features/user/profile/api.ts` — `withdrawAccount` 함수
  - `apps/web/src/features/user/profile/` — `useWithdraw` mutation hook

## Risks

- **BE user 모듈 전무**: `apps/api/src/modules/user/` 전체가 `.gitkeep` stub. 모듈 초기 구성 + UserRepository 구현이 선행 필요. 다른 user plan과 병렬 작업 시 merge 충돌 가능.
- **소셜 재인증(D-2)**: 소셜 유저의 `oAuthAccessToken` 획득 방법이 FE에서 불명확. Kakao/Google SDK 재인증 플로우 없으면 소셜 탈퇴는 별도 구현 필요. MVP에서 소셜 탈퇴 범위를 패스워드 재확인 없이 처리할지 결정 필요.
- **파트너 capability 검증(D-3)**: 요구사항은 `APPROVED` Partner 보유 시 파트너 해지를 선행 요구하나, API명세에 해당 에러 코드 미정. BE에서 단순 400으로 막을지, 에러 코드 확정 필요.
- **익명화 전략**: 요구사항은 "해시 식별자로 치환"만 정의. 정확한 포맷(prefix/hash 길이) 미정. 5년 보관 후 DELETED 전이 스케줄러는 MVP Out이나 `withdrawnAt` 필드는 반드시 기록 필요.
- ~~**진입점 위치(D-1)**~~ — RESOLVED: Figma "마이 - 회원탈퇴 - 모달"(8505:19284)에서 진입점 = 개인 정보 수정 화면(`/my/profile`) 하단 푸터 "탈퇴하기" 링크로 확정. 단, `ProfileEditScreen`(개인 정보 수정) 자체가 아직 미구현(#185 미완)이라 진입 링크는 그 화면 구현과 함께 추가해야 함.

## Validation

- Tests:
  - BE 단위: 진행 중 예약 존재 시 400, 비밀번호 불일치 400, 미인증 401, 정상 탈퇴 시 user.status=WITHDRAWN + refreshToken 삭제 + 쿠키 만료.
  - FE: useWithdraw 성공 시 accessToken localStorage 제거 + 라우팅, 에러별 토스트 노출.
- Manual checks:
  - 이메일 유저: 비밀번호 정확 입력 → 200 → accessToken 사라짐 → 탈퇴 완료 페이지 이동.
  - 이메일 유저: 비밀번호 오입력 → 400 `PASSWORD_MISMATCH` → 토스트 노출.
  - 진행 중 예약 보유 유저 → 400 `ACTIVE_RESERVATIONS_OR_ARTWORKS_EXIST` → 토스트 노출.
  - 탈퇴 후 동일 accessToken으로 `GET /users/me` → 401.
  - 탈퇴 후 동일 이메일로 로그인 → 404 또는 401.
- Observability: 탈퇴 완료 로그(`userId`, `withdrawnAt`, `provider`) — 운영 모니터링 필수.

## Open decisions (사람 결정 대기 — 추측 금지)

- **D-1. 탈퇴 진입점 위치 — [RESOLVED 2026-06-05, Figma "마이 - 개인 정보 수정" 8505:19263 + "마이 - 회원탈퇴 - 모달" 8505:19284]**: 진입점 = **개인 정보 수정 화면(`/my/profile` = `ProfileEditScreen`) 하단 Divider 링크**. `MyPageHub.tsx`/고객지원 섹션 아님.
  - 정확한 위치: Divider(8505:19278), 예약자 정보 섹션 아래·고정 `ButtonSection`("저장") 위. `flex` 가운데 정렬, padding 20.
  - 카피: 안내문 "혹시 리듬을 떠나고 싶으신가요?"(12 Regular) + "탈퇴하기"(12 SemiBold) 링크. 둘 다 `#7D838D` = `foreground-tertiary`.
  - "탈퇴하기" 클릭 → 확인 모달(8505:19302/19303) 오픈.
  - **선행 의존**: `ProfileEditScreen`(개인 정보 수정 화면) 자체가 미구현(#185) — 화면 구조(계정 정보: 계정 disabled + 닉네임 + "비밀번호 재설정" 링크 / 예약자 정보: 이름·연락처 / 하단 "저장" 고정 버튼)는 #185 소관. 회원탈퇴는 이 화면의 Divider 링크 + 모달 + DELETE 호출만 담당.
- **D-2. 소셜 유저 oAuthAccessToken 수집 방법**: 소셜 탈퇴 시 FE에서 카카오/구글 SDK 재인증을 통해 토큰 획득해야 하나, 현재 OAuth 플로우는 인가코드 방식(`POST /auth/oauth/kakao`). 탈퇴 전용 재인증 흐름 별도 필요. MVP에서 소셜 탈퇴 비밀번호 확인 대신 단순 "탈퇴 의사 확인"으로 처리할지, 또는 소셜 unlink API 호출 생략할지 결정 필요.
- **D-3. 파트너 capability 보유 시 에러 코드**: 요구사항에 "진행 중 Partner capability 존재" 실패 사유 명시. API명세에 해당 에러 코드 없음. `ACTIVE_PARTNER_EXISTS` 등 코드 정의 + 프론트 메시지("파트너 해지 후 탈퇴 가능합니다") 확정 필요. BE 처리 순서(파트너 체크 vs 예약/작품 체크 순서)도 결정 필요.
- **D-4. 탈퇴 완료 후 이동 — [RESOLVED 2026-06-05]**: 별도 완료 페이지 없음. 탈퇴 200 성공 → localStorage `accessToken` 제거 → **메인화면(`/` = `app/page.tsx`)으로 `router.push('/')`**. (완료 토스트로 안내만, 전용 `/withdrawn` 페이지 미생성.)
- **D-5. 5년 후 DELETED 스케줄러 MVP 포함 여부**: 요구사항 정의되어 있으나 API명세 미포함. `withdrawnAt` 기록은 필수이나 스케줄러 구현은 MVP 이후 가능. 명시적 Out 확정 필요.

## Decision Log

- contract 스냅샷은 Notion API명세 `DELETE /users/me` 기준 고정. 요구사항(auth §6)과의 차이(스케줄러·30일 재가입·알림 철회·OAuth unlink 등) 중 API명세에 포함된 것만 contract에 포함. 나머지 D-2~D-5로 분리.
- BE user 모듈 전무 확인(코드 직접 grep). BE는 scratch에서 모듈 생성 필요.
- MSW mock `DELETE /users/me` 미존재 확인. FE는 mock 추가 후 개발 가능.

## Outcome

- Status: planning. **D-1·D-4 RESOLVED**(2026-06-05). D-1=진입점 개인 정보 수정 화면 하단 Divider "탈퇴하기" + 확인 모달 스펙 확정. D-4=별도 완료 페이지 없음, 성공 시 메인 `/` 이동. 잔여 대기: D-2(소셜 토큰)·D-3(파트너 에러코드)·D-5(스케줄러). BE는 user 모듈 전무 → be-first. FE 진입 링크는 `ProfileEditScreen`(#185 미완) 구현과 묶여야 함.
- Follow-up: 마이페이지 plan D9도 D-1 결과(진입점=개인 정보 수정 화면 푸터)로 동시 업데이트 필요.
