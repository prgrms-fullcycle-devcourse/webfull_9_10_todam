# Feature Plan: 유저 마이 - 찜한 공방 목록 조회, 공방 찜 등록/해제 (user / store)

## Summary

- Goal: 인증된 User가 (1) 본인이 찜한 공방 목록을 최신 찜 등록순으로 무한 스크롤 조회하고(공방명·대표 이미지·주소 + 공방 상세 이동), (2) 공방 찜을 **토글(등록/해제)** 한다. 찜 변경은 즉시 반영(낙관적 업데이트) + 목록 갱신, 공방 상세의 찜 상태와 동기화.
- Owner: (BE 미정 / FE 미정)
- Date: 2026-06-02

## Status

<!--
게이트가 읽는 체크리스트. 셋 다 [x] 여야 completed/ 이동 가능 (pre-commit이 강제).
- API 구현: 실 BE(`apps/api`) 엔드포인트가 contract대로 존재·동작. MSW mock만 있으면 미체크.
- UI 구현: 화면/컴포넌트 구현 완료.
- API 연동: **실 API** 요청/응답이 contract 스키마로 연결.
-->

- [ ] API 구현
- [ ] UI 구현
- [ ] API 연동

## Context

<!-- 정본은 메인이 붙여넣은 Notion DB 발췌. -->

- 요구사항명세서(고정): `docs/requirements.md`
  - `접근 주체` — `User` 이상(`@UseGuards(AuthGuard)`). 찜 기능은 로그인 필수.
  - `공방 store` §1 공방 상태 / §4 공방 조회 — 찜 목록은 `PUBLISHED` 공방만 노출(삭제·비공개·`SUSPENDED`/`DRAFT`/`PENDING` 제외).
- 기능명세: "찜한 공방 목록 조회", "공방 찜 등록 / 해제" (Notion 기능명세 DB — 메인이 정본 발췌 제공)
- API명세 (정본 SSOT — 메인이 정본 발췌 제공):
  - `POST /stores/{storeId}/favorite` — 공방 찜 등록 및 해제 (토글)
  - `GET /users/me/favorite-stores?cursor&limit` — 찜한 공방 목록 조회
- Notion 게시 링크: `<TODO: 메인이 별도 첨부 예정 — notion.site URL placeholder>`
- 선행 plan(SSOT 재사용): `docs/exec-plans/active/유저 예약 - 예약 상세조회.md`(공통 봉투 / 401 처리 / 커서 페이지네이션 mock 토글 패턴), `docs/exec-plans/active/user-예약-나의 예약조회.md`(`useInfiniteQuery` + IntersectionObserver 무한 스크롤 패턴).

### 접근주체/가드

- `POST /stores/{storeId}/favorite` / `GET /users/me/favorite-stores` 둘 다 `User` 이상(`@UseGuards(AuthGuard)`). 미인증 시 401.
- 찜 등록 대상은 `PUBLISHED` 공방만(요구사항 store §1·§4). 삭제·비공개 공방 찜·조회 불가 → 404/409(스펙 미정 — D4).

## API Contract (스냅샷)

<!-- 정본(Notion API명세) 기준 고정. BE/FE/reviewer 가 바인딩하는 SSOT.
     기존 like.ts 자산과 drift 있음 — Decisions D1 참조. 신규 contract 는 favorite.ts 로 신설. -->

### 데이터모델 (apps/api/prisma/schema.prisma — 신규/연관 추정)

찜 도메인 모델은 정본 요구사항에 별도 엔티티 정의가 없으나 API명세 시스템 처리("favorite_stores join stores")로 추정:

| 모델 | 필드 | 비고 |
|---|---|---|
| `FavoriteStore` (`favorite_stores`) | `id`(uuid, = 응답 `favoriteId`), `userId`(uuid, FK User), `storeId`(uuid, FK Store), `createdAt`(DateTime) | `@@unique([userId, storeId])` — 중복 찜 불가(기능명세 "중복 찜 불가"). 토글 시 이력 없으면 생성, 있으면 삭제. |
| `Store` (`stores`) (연관) | `id`, `name`, `category`, `status`(StoreStatus), 대표 이미지(thumbnail), `address` | join 대상. 목록 응답은 **`status = PUBLISHED`** 만 반환. `category` 는 D2-b 로 응답 포함. |

`enum StoreStatus`(기존 `@todam/shared`): `DRAFT`, `PENDING`, `PUBLISHED`, `SUSPENDED`.

### 엔드포인트

#### 1) `POST /stores/{storeId}/favorite` — 공방 찜 등록 및 해제 (토글)

찜 이력 없으면 생성(`isFavorite: true`), 있으면 제거(`isFavorite: false`). **Request body 없음** (path param `storeId` 만).

**Headers:** `Content-Type: application/json`, `Accept: application/json`, `Authorization: Bearer {accessToken}`
**Path:** `storeId: string(uuid)`
**Request body:** 없음.

**시스템 처리:**
1. 토큰으로 사용자 식별 (AuthGuard). 미인증 시 401.
2. `storeId` 공방 조회 — 비존재/비공개(`!= PUBLISHED`) 시 404(또는 409 — D4 미정).
3. `(userId, storeId)` 찜 이력 조회 → 없으면 생성(`isFavorite: true`), 있으면 삭제(`isFavorite: false`).

**200 OK (정본 그대로):**
```json
{ "storeId": "store-uuid-001", "isFavorite": true }
```
> 정본 예시는 `data` 객체 본문만 제시. 공통 봉투 `{ statusCode, timestamp, path, message, data, error }` 의 `data` 에 위 객체가 들어가는 형태로 가정(다른 엔드포인트 정합 — D6).

| 필드 | 타입 | 출처 | 비고 |
|---|---|---|---|
| `storeId` | `string(uuid)` | path param echo | |
| `isFavorite` | `boolean` | 토글 결과 | `true` = 등록됨 / `false` = 해제됨 |

**에러:**

| status | code | message | 비고 |
|---|---|---|---|
| 401 | `UNAUTHORIZED` | "찜하기 기능을 이용하려면 로그인이 필요합니다." | 정본 그대로 |
| 404 / 409 | (정본 본문 비어있음 — **스펙 미정**) | — | 삭제·비공개 공방 찜 불가. D4 |
| 500 | `INTERNAL_SERVER_ERROR` | (정본) | |

#### 2) `GET /users/me/favorite-stores?cursor&limit` — 찜한 공방 목록 조회

커서 기반 페이지네이션. `favorite_stores join stores`, **`PUBLISHED` 공방만** 반환, 최신 찜 등록순 정렬(기능명세 "최신 찜 등록순").

**Headers:** `Accept: application/json`, `Authorization: Bearer {accessToken}`
**Query:** `cursor?: string`(선택), `limit?: number`(기본 10 — 정본. 단 팀 공통 상수 `DEFAULT_PAGE_SIZE`(20) 와 drift — D7)

**200 OK (정본 그대로):**
```json
{
  "favoriteStores": [
    {
      "favoriteId": "fav-uuid-101",
      "storeId": "store-uuid-001",
      "name": "토담 공방 성수점",
      "category": "도자기",
      "imageUrl": "https://cdn.todam.app/stores/todam-studio/01_thumb.jpg",
      "address": "서울특별시 성동구 성수이로 12길",
      "createdAt": "2026-05-26T12:00:00.000Z"
    }
  ],
  "nextCursor": "fav-uuid-101"
}
```
> 정본 예시는 `category` 미포함이나, **D2-b 결정으로 추가**(카드 카테고리 태그 렌더용 + 타 도메인 정합 — `reservation-list` 가 이미 `category: z.string()` 사용). BE 응답에 `Store.category` 추가 요청 필요.

| 필드 | 타입 | 출처 | 비고 |
|---|---|---|---|
| `favoriteStores[].favoriteId` | `string(uuid)` | `FavoriteStore.id` | 목록 key / 커서 단위 |
| `favoriteStores[].storeId` | `string(uuid)` | `Store.id` | 공방 상세 이동 / 찜 토글 대상 |
| `favoriteStores[].name` | `string` | `Store.name` | 공방명 |
| `favoriteStores[].category` | `string` | `Store.category` | **D2-b 추가** — 카드 카테고리 태그(예 "도자기"). `reservation-list.category` 와 동일 컨벤션(`z.string()` 자유 문자열) |
| `favoriteStores[].imageUrl` | `string` | `Store` 대표 이미지(썸네일) | 기능명세 "대표 이미지는 썸네일" |
| `favoriteStores[].address` | `string` | `Store.address` | 위치 표시 |
| `favoriteStores[].createdAt` | `string(ISO8601)` | `FavoriteStore.createdAt` | 정렬 기준(최신 찜순) |
| `nextCursor` | `string \| null` | 마지막 항목 `favoriteId` 또는 null | 다음 페이지 없음 = null. **`hasMore` 필드 없음** — reservation-list 와 drift(D7) |

**에러:**

| status | code | message | 비고 |
|---|---|---|---|
| 400 / 401 / 404 / 409 | (정본 본문 비어있음 — **미정**) | — | 401 은 공통 `UNAUTHORIZED` 보정 가정 |
| 500 | `INTERNAL_SERVER_ERROR` | (정본) | |

### zod 스키마 제안 (`packages/shared/src/contracts/favorite.ts` 신설)

```ts
// 공방 찜 토글 (POST /stores/:storeId/favorite — body 없음).
export const toggleFavoriteResultSchema = z.object({
    storeId: z.string(),
    isFavorite: z.boolean(),
});
export type ToggleFavoriteResult = z.infer<typeof toggleFavoriteResultSchema>;

// 찜한 공방 목록 (GET /users/me/favorite-stores).
export const favoriteStoreItemSchema = z.object({
    favoriteId: z.string(),
    storeId: z.string(),
    name: z.string(),
    category: z.string(), // D2-b: 카드 카테고리 태그(예 "도자기"). reservation-list.category 와 동일 컨벤션.
    imageUrl: z.string(),
    address: z.string(),
    createdAt: z.string(), // ISO 8601
});
export type FavoriteStoreItem = z.infer<typeof favoriteStoreItemSchema>;

export const favoriteStoreListResultSchema = z.object({
    favoriteStores: z.array(favoriteStoreItemSchema),
    nextCursor: z.string().nullable(),
});
export type FavoriteStoreListResult = z.infer<typeof favoriteStoreListResultSchema>;
```
> `favorite.ts` 가 기존 `like.ts` 를 **대체**한다(D1). `packages/shared/src/index.ts` 의 `export * from './contracts/like';` 는 `favorite` 로 교체.

## 에러 매핑 (FE)

| 상황 | 처리 |
|---|---|
| 401 (목록·토글 공통) | `/login` 리다이렉트 (선행 plan `ReservationsListClient` 패턴 재사용, `ApiError.statusCode === 401`) |
| 500 / 네트워크 오류 (목록) | "찜한 공방을 불러오지 못했습니다." 안내 + 재시도 |
| 토글 성공 (해제) | 목록에서 즉시 제거 + `"'{공방명}' 찜 해제되었어요"` 토스트 (D3 확정, 언두 없음) |
| 500 / 토글 실패 | 낙관적 업데이트 **롤백**(항목 복원) + 토스트("찜 상태 변경에 실패했습니다.") (D3) |
| 404/409 (토글 — 비공개 공방) | 롤백 + 토스트("이미 비공개되었거나 삭제된 공방입니다.") — D4 확정 후 메시지 고정 |
| 빈 목록 | 빈 상태 화면(`EmptyState`, "아직 찜한 공방이 없어요." 등 — empty 디자인만 대기 D5) |

## Design tokens (Figma 정본 — node `8505:19308` "마이 - 찜한 공방 목록")

> 메인이 붙여넣은 Figma JSON 발췌. 카드·토스트 정본 확보 → D2/D3 일부 해소(아래). **단 빈 상태(empty) 화면은 본 프레임에 없음 — 여전히 미확보(D5).**

- **Frame**: 360×812, bg `#FBF8F3`. 세로 스택(Header → main → ToastRender).
- **Header** (`type=sub`, h60): 좌측 back 버튼 56×56 r16 ghost, chevron-left 아이콘 24×24 `#191E25`. 타이틀 "찜한 공방 목록" 18px Pretendard Medium(500) `#191E25` lh24. (우측 close 버튼은 hidden.)
- **main / section-store**: 좌우 padding 16 → 콘텐츠 폭 328. section paddingY 8 / gap 8.
- **LikedStoreList**: 카드 간 gap **16**.
- **LikedStoreItem (카드)**: 328×80, 가로 배치, gap 12, 세로 center. 표시 필드 = **썸네일 + 카테고리 태그 + 공방명 + 주소 + 찜 버튼**. (평점·리뷰수·대표 클래스 **없음** → D2. 카테고리 태그는 `category` 필드로 렌더 → D2-b.)
  - **썸네일(IMG)**: 80×80, r12, placeholder bg `#F1F2F4`. 카테고리 태그(r4, bg `#F1F2F4`, 10px Medium `#434A54`) — `category` 값 렌더.
  - **텍스트 블록**: gap 8. 공방명 16px SemiBold(600) `#191E25` lh20 / 주소 12px Regular(400) `#7D838D` lh16 (1~2줄).
  - **찜 버튼**: 40×40, r999(완전 원형), bg `#0E1218` @30%, heart-fill 아이콘 20×20 `#FFFFFF`. style=overlay size=sm. **목록 내 항목은 항상 채워진(liked) 하트** — 탭 시 해제.
- **Toast** (ToastRender 하단, paddingBottom 112): 328×48 r12, bg `#0E1218` @90%. info 아이콘 16×16 white + 메시지 16px Regular `#FFFFFF` `"'{공방명}' 찜 해제되었어요"`. gap 8, padding(16/12). **선택적 "취소"(undo) 버튼** 컴포넌트 프로퍼티 존재(이 인스턴스는 hidden) → **해제 언두 패턴 정본 근거(D3).**

## FSD 파일 배치 계획

```
packages/shared/src/
  contracts/
    favorite.ts                         (신설 — toggleFavoriteResult + favoriteStore 목록 스키마)
    like.ts                             (제거 또는 @deprecated — D1)
  index.ts                              (수정 — like → favorite 재노출 교체)

apps/web/src/
  features/store/
    toggle-favorite/                    (신설 — 기존 toggle-like 대체. D1)
      api.ts                            POST /stores/:storeId/favorite (body 없음)
      queries.ts                        useToggleFavorite (낙관적 업데이트 + 롤백, queryClient 무효화)
      index.ts
      ui/FavoriteToggleButton.tsx       (기존 LikeToggleButton 이관 — POST/isFavorite shape)
    toggle-like/                        (제거 — D1. favorites page + 공방 상세에서 import 경로 교체)
    favorite-list/                      (신설 — 목록 페치/무한스크롤 wiring)
      api.ts                            GET /users/me/favorite-stores?cursor&limit
      queries.ts                        useFavoriteStores (useInfiniteQuery, getNextPageParam = nextCursor)
      index.ts
  entities/store/ui/
    FavoriteStoreCard.tsx               (신설 — 찜 목록 카드: 썸네일 + name + address + 찜 버튼 + 공방 상세 이동. 행동0=표현, 토글 버튼은 features 컴포넌트 주입)
  app/(user)/my/favorites/
    page.tsx                            (교체 — 현재 MOCK 배열 하드코딩. 서버 컴포넌트, 클라이언트 위임)
    _components/
      FavoritesListClient.tsx          (신설 — useInfiniteQuery + IntersectionObserver + 401 리다이렉트 + 빈/에러 상태)

apps/web/src/mocks/
  handlers.ts                           (수정 — /like → /favorite 교체, favorite-stores 목록 핸들러 추가)
  db.ts                                 (수정 — likedStores 유지/확장 + favorite-stores seed + 커서 페이지네이션)
```

세그먼트 컨벤션: 기존 코드베이스 flat(`api.ts` + `queries.ts` + `ui/`) 따름(세그먼트 폴더 아님).

## MSW / db mock 계획

1. **handlers.ts**
   - 기존 `http.post('*/api/v1/stores/:storeId/like')` → `http.post('*/api/v1/stores/:storeId/favorite')` 로 **교체**. body 없음, `toggleFavorite(storeId)` 호출 → `{ storeId, isFavorite }` 반환.
   - 신규 `http.get('*/api/v1/users/me/favorite-stores')` 추가 — `cursor`/`limit` 쿼리 파싱, `nextCursor` 만 반환(`hasMore` 없음 — D7). 시뮬 토글: `?unauth=1` → 401, `?empty=1` → 빈 목록, `?simulate=500` → 500.
2. **db.ts**
   - `likedStores: Set<string>` 유지하되 `toggleFavorite(storeId): boolean` (이력 없으면 add → true, 있으면 delete → false) 신설. 기존 `setLike(storeId, liked)` 는 제거(POST 토글로 일원화 — D1).
   - `SEEDED_FAVORITE_STORES: FavoriteStoreItem[]` seed 추가(최신 찜순, `PUBLISHED` 공방만). 토글 시 목록 즉시 반영(add/remove 동기화). 빈 상태/페이지네이션 검증용 ≥ 12개 권장.
   - `listFavoriteStores(cursor, limit)` — `createdAt` 내림차순 + `favoriteId` 커서 슬라이스, `nextCursor` 산출.

## Decisions (D 항목)

- **D1 — `like` → `favorite` DRIFT (정본 우선, 기존 자산 교체)**
  - 기존 `packages/shared/src/contracts/like.ts`: `PATCH` body `{ liked }`, 응답 `{ storeId, liked }`, 엔드포인트 `/stores/:id/like`.
  - 정본 API명세: `POST /stores/:id/favorite`, **body 없음**, 응답 `{ storeId, isFavorite }`.
  - **drift 3건**: 메서드(PATCH→POST), 응답 필드(`liked`→`isFavorite`), 엔드포인트(`/like`→`/favorite`).
  - **사용처 전수 조사(2026-06-02)**: 기존 `like` 자산의 실제 소비자는 **`app/(user)/my/favorites/page.tsx` 단 한 곳**(MOCK 하드코딩 임시 placeholder, 이번 기능이 통째로 교체할 화면). 공방 상세 등 다른 화면 사용처 **0건**(grep 확인). 실 BE 연동 없음(MSW only).
  - **✅ 확정 (2026-06-02, 사용자 선택 A)**: **즉시 삭제·교체**. `like.ts` 삭제 → `favorite.ts` 신설, `index.ts` 재노출 교체. `features/store/toggle-like/` → `toggle-favorite/` 교체(`LikeToggleButton`→`FavoriteToggleButton`, `useToggleLike`→`useToggleFavorite`, `toggleLike`→`toggleFavorite`). MSW `POST /like`+`setLike`+`likedStores` → `POST /favorite`+`toggleFavorite`. favorites page 신규 작성(어차피 이번 범위). deprecate 단계 불필요(유일 소비자가 교체 대상이라 죽은 코드 두 벌 유지 이득 0).
- **D2 — 목록 응답 vs 기능명세 표시필드 격차 (BE 필드 추가 vs FE 표시 생략)**
  - 기능명세는 목록 카드에 **평점 / 리뷰 수 / 공방 대표 클래스 정보 / 사용자 찜 상태** 표시를 요구.
  - 정본 `favorite-stores` 응답은 `favoriteId / storeId / name / imageUrl / address / createdAt` 만 — **평점·리뷰수·대표 클래스 없음**. (찜 상태는 목록에 있는 모든 항목이 찜됨이므로 카드의 찜 버튼은 항상 `isFavorite: true` 로 가정.)
  - **Figma 정본 렌더(`8505:19308` + 마이 Description 표) 확인 결과**: 찜 목록 카드는 **썸네일 + 공방명 + 주소 + 찜 버튼**만 렌더. **평점·리뷰수·대표 클래스 UI 없음.** 카테고리 태그도 실제 렌더 화면에는 노출 안 됨(컴포넌트 내부 placeholder 잔재).
  - **✅ 확정 (UI 기준, 2026-06-02)**: 옵션 **(B)** — FE 는 평점/리뷰수/대표클래스 **표시 생략**. 카드 = 썸네일 + 공방명 + 주소 + (카테고리 태그) + 찜 버튼.
  - **D2-b(카테고리) 확정: `category` 추가** — 카드에 카테고리 태그를 렌더하므로 BE 응답에 `Store.category` 추가 요청. 타 도메인(`reservation-list.category`)이 이미 사용 중이라 정합. → contract 는 **7필드**(`favoriteId/storeId/name/category/imageUrl/address/createdAt`). (사용자 확인: "다른 곳도 추가한 듯".)
- **D3 — 낙관적 업데이트(Optimistic Update) 채택 여부**
  - 기능명세: "변경 즉시 반영", "등록 시 목록 즉시 반영, 해제 시 목록에서 제거", **"낙관적 업데이트 적용 여부 정의 필요"**.
  - 기존 `LikeToggleButton` 은 이미 로컬 state 기반 낙관적 토글 + 롤백 구현.
  - **Figma 정본 렌더 + Description #11 확인 결과**: #11 "찜한 공방 목록 해제 Btn → 클릭 시 찜한 공방 해제 / **상태 변경 반영 완료 시 toast 노출**". 토스트 `"'{공방명}' 찜 해제되었어요"`. **취소(undo) 버튼은 렌더 화면에서 숨김**(컴포넌트 프로퍼티만 존재, 디자인 미사용).
  - **✅ 확정 (UI 기준, 2026-06-02)**: 낙관적 업데이트 **채택, 언두 미포함**. ① 토글 버튼 즉시 아이콘 반전. ② **목록에서 해제 시 즉시 제거**(react-query `setQueryData` 로 무한쿼리 페이지에서 항목 제거). ③ **상태 변경 반영 완료(성공) 시 `"'{공방명}' 찜 해제되었어요"` 토스트 노출**(undo 버튼 없음). ④ 실패 시 자동 복원(항목 되돌림) + 에러 토스트. 재찜(등록) 시 토스트 문구는 디자인 미정 → "'{공방명}' 찜 등록되었어요" 가정(D3-b, 사소).
- **D4 — 토글 404/409 스펙 미정**
  - 정본: 삭제·비공개 공방 찜 불가하나 404/409 응답 본문이 비어있음(미정). 목록 조회 400/404/409 도 미정.
  - **결정 필요**: BE 가 비공개/삭제 공방 찜 시 404 인지 409 인지, error code·message 확정. FE 토스트 메시지·롤백 분기 고정에 필요. **BE 답변 대기.**
- **D5 — 빈 상태 화면만 미확보 (카드/목록/토스트는 Figma 정본 확보)**
  - **확보됨**: 목록 화면(`8505:19308`) — 카드 레이아웃(328×80, 썸네일 80×80 r12, 공방명 16/SemiBold, 주소 12/Regular, 찜 버튼 40×40 r999 overlay@30% heart-fill), 카드 gap 16, 콘텐츠 padding 16, 해제 토스트(§Design tokens 참조).
  - **여전히 미확보**: **빈 목록(empty state) 화면** — 본 프레임에 없음. 무한스크롤 로딩 인디케이터 디자인도 미확보.
  - **결정 필요**: 빈 상태 Figma 정본 확보(문구·일러스트·CTA). 미확보 시 카드/목록은 착수 가능하나 빈 상태는 임시 placeholder 후 디자인 확정 시 교체.
- **D6 — 토글 응답 봉투 형태**
  - 정본 200 예시가 `data` 객체 본문(`{ storeId, isFavorite }`)만 제시. 다른 엔드포인트(`ok()` 공통 봉투)와 정합 위해 `data` 에 래핑 가정.
  - **결정(보정 가정)**: 공통 봉투 `data` 에 `{ storeId, isFavorite }`. BE 확인 시 고정.
- **D7 — 목록 페이지네이션 `hasMore` 부재 (reservation-list 와 drift)**
  - reservation-list contract 는 `{ ..., nextCursor, hasMore }`. favorite-stores 정본은 `nextCursor` 만(`hasMore` 없음).
  - **결정(보정 가정)**: `nextCursor === null` 이면 다음 페이지 없음으로 판정(`getNextPageParam` 이 `nextCursor ?? undefined`). `hasMore` 추가 요청 불필요. limit 기본값은 정본 10 vs 팀 공통 `DEFAULT_PAGE_SIZE`(20) drift — reservation-list 선례(공통 상수 우선) 따라 **20 권장**, BE 확인.

## Open questions

1. ~~(D1) like 즉시 삭제 vs deprecate~~ → **확정: 즉시 삭제·교체(A)**. 사용처 favorites page 1곳뿐(교체 대상), 타 화면 0건.
2. ~~(D2) 카드 표시 필드~~ → **확정**: 옵션 B(평점/리뷰수/클래스 생략). **D2-b → `category` 추가**(7필드), 카드 카테고리 태그 렌더. BE 응답에 `Store.category` 추가 요청.
3. ~~(D3) 언두 포함 여부~~ → **확정**: 낙관적 제거 + 완료 토스트, **언두 미포함**(취소 버튼 디자인 미사용). UI 기준.
4. (D4) 비공개/삭제 공방 찜 시 404 vs 409 + error code·message 정본 확정(토글/목록 공통). **BE 대기.**
5. (D5) **빈 목록(empty state)** Figma 정본 — 문구·일러스트·CTA(카드/목록/토스트는 확보됨). 미확보 시 임시 placeholder.
6. (D7) `limit` 기본값 10(정본) vs 20(팀 공통 `DEFAULT_PAGE_SIZE`)?

## Scope

- In:
  - shared: `packages/shared/src/contracts/favorite.ts` 신설(zod) + `index.ts` 재노출 교체.
  - BE: `POST /stores/{storeId}/favorite`(토글, AuthGuard, PUBLISHED 가드, 200/401/404|409/500), `GET /users/me/favorite-stores`(AuthGuard, PUBLISHED join, 최신 찜순, 커서 페이지네이션, 200/401/500).
  - FE: 찜 목록 화면(`/my/favorites`) 무한 스크롤 + 빈/에러 상태 + 401 리다이렉트, 찜 토글 버튼(낙관적 업데이트 + 롤백), 공방 상세 찜 상태 동기화(쿼리 무효화), 목록에서 해제 시 즉시 제거(D3 확정 시).
  - mock: MSW `/favorite` 토글 + `favorite-stores` 목록 핸들러 + db seed/커서.
- Out:
  - 공방 상세 조회 화면 자체(별도 plan) — 본 plan 은 찜 버튼/상태 동기화 wiring 까지만.
  - 평점/리뷰수/대표 클래스 표시(D2 BE 확장 결정 전까지 범위 외).
  - 찜 알림(notification 도메인).
  - 파트너용/Guest 찜(찜은 User 전용).

## Plan

1. (선결정) Open decisions D1~D7 사람 결정 수령 → 본 plan contract 스냅샷·범위 확정(특히 D1 like 처리 / D2 표시필드 / D5 디자인).
2. (shared) `packages/shared/src/contracts/favorite.ts` 신설 — `toggleFavoriteResultSchema` + `favoriteStoreItemSchema` + `favoriteStoreListResultSchema`. `index.ts` 에서 `like` → `favorite` 재노출 교체. (D1 deprecate 단계 결정 반영.)
3. (mock) `db.ts` — `toggleFavorite(storeId)` + `SEEDED_FAVORITE_STORES` seed + `listFavoriteStores(cursor, limit)`. `handlers.ts` — `/like` → `/favorite` 교체 + `GET /users/me/favorite-stores` 추가(시뮬 토글 `unauth/empty/500`).
4. (FE features) `features/store/toggle-favorite/{api,queries,index,ui/FavoriteToggleButton}` 신설(기존 toggle-like 이관) + `features/store/favorite-list/{api,queries,index}` 신설(`useInfiniteQuery`, `getNextPageParam = nextCursor ?? undefined`).
5. (FE entities) `entities/store/ui/FavoriteStoreCard.tsx` — 썸네일 + name + address + 공방 상세 이동 + 찜 버튼 슬롯.
6. (FE page) `app/(user)/my/favorites/page.tsx` 교체(서버 컴포넌트) + `_components/FavoritesListClient.tsx`(무한스크롤 + IntersectionObserver + 401 리다이렉트 + 빈/에러 상태 + 해제 즉시 제거 D3).
7. (BE) `apps/api` store/favorite use-case + controller(AuthGuard, PUBLISHED 가드, 토글 upsert/delete, 목록 join + 커서). 예외 404|409(D4)/401/500.
8. 연동 검증: 토글 후 목록·공방 상세 찜 상태 동기화, 무한스크롤 커서 끝(nextCursor=null), 빈 목록, 401 리다이렉트, 낙관적 업데이트 롤백.

## Out (단계별 완료물)

- API: <!-- 구현된 엔드포인트, 파일 -->
- UI: <!-- 구현된 화면, 컴포넌트 -->
- 연동: <!-- 연결 지점, 검증 결과 -->

## Risks

- **like → favorite 마이그레이션(D1)** 이 기존 자산(`like.ts`, `toggle-like`, `setLike`, MSW `/like`, `LikeToggleButton`) 전반에 걸침. 공방 상세 등 다른 사용처 import 경로 동시 교체 누락 시 빌드/런타임 깨짐. 사용처 전수 확인 필요.
- **D2 표시필드 격차** 미해소 시 카드 UI 확정 불가(평점/리뷰수 노출 여부). 정본 6필드로 갈지 BE 확장할지 선결정 필수.
- **D5 디자인 미확보** → FE 카드/빈 상태/무한스크롤 착수 불가. 디자인 대기.
- **D4 404/409 미정** → 토글 실패 분기(비공개 공방) 메시지 고정 불가. BE 답변 대기.
- 토글이 **body 없는 POST** 이므로 기존 `apiFetch({ body })` 호출부에서 body 생략 처리 확인 필요.
- 낙관적 업데이트 + 무한쿼리 캐시 직접 조작(`setQueryData`) 시 페이지 경계 정합(중복/누락) 주의.

## Validation

- Tests:
  - BE: 토글 생성/삭제 분기(이력 유무), `@@unique` 중복 방지, PUBLISHED 가드(비공개 공방 404|409), 목록 PUBLISHED 필터 + 최신 찜순 + 커서, 401.
  - shared zod: 정본 응답 fixture 파싱 통과 + 잘못된 shape 거부.
  - FE: `useInfiniteQuery` 페이지 병합, 토글 낙관적 업데이트 + 롤백, 401 리다이렉트, 빈/에러 상태 렌더.
- Manual checks:
  - 로그인 후 `/my/favorites` 진입 → 목록 노출 → 스크롤 하단 다음 페이지 로드 → 끝(nextCursor=null) 정지.
  - 카드 찜 해제 → 즉시 목록에서 제거(D3) / 실패 시 복원.
  - 공방 상세 찜 토글 → `/my/favorites` 재진입 시 동기화.
  - 빈 목록(`?empty=1`) → 빈 상태 화면.
  - 미로그인(`?unauth=1`) → `/login` 리다이렉트.
- Observability: 500 로깅(목록·토글), 토글 실패율 메트릭(가능하면).

## Decision Log

- 2026-06-02: 기능명세 2건(찜 목록 조회 / 찜 토글) + API명세 2건(`POST /stores/{storeId}/favorite`, `GET /users/me/favorite-stores`) 정본 발췌 수령. contract 스냅샷 정본 기준 고정.
- 2026-06-02: **기존 like 자산과 정본 drift 3건 식별(D1)** — PATCH→POST / `liked`→`isFavorite` / `/like`→`/favorite`. 정본 우선, `favorite.ts` 신설 + `like.ts` 교체 방향(사람 승인 대기).
- 2026-06-02: **목록 표시필드 격차(D2)** — 기능명세 요구(평점/리뷰수/대표클래스) vs 정본 응답 6필드. Open decision, 추측 금지.
- 2026-06-02: **낙관적 업데이트(D3)** — 기능명세가 "적용 여부 정의 필요" 명시. 권장 채택(롤백 포함) + 목록 즉시 제거 UX 사람 확정 대기.
- 2026-06-02: 404/409 스펙 미정(D4), 봉투 형태(D6), `hasMore` 부재·limit 기본값 drift(D7) Open decision 으로 박음.
- 2026-06-02: **Figma 정본(`8505:19308` "마이 - 찜한 공방 목록") 수령** — §Design tokens 채움. ① 카드 6필드만 표시(평점/리뷰수/클래스 없음) → D2 옵션 B 지지(카테고리 태그는 D2-b 소격차). ② 해제 토스트 + 취소(undo) 프로퍼티 → D3 낙관적 제거 + 언두 패턴 지지. ③ D5 는 빈 상태 화면만 미확보로 축소(카드/목록/토스트 확보).
- 2026-06-02: **UI 기준 결정 확정** (마이 Description 표 #11 "찜 해제 → 상태 변경 반영 완료 시 toast 노출", 렌더 화면). **D2 = 옵션 B 확정**(평점/리뷰수/대표클래스 생략). **D3 = 낙관적 제거 + 완료 토스트, 언두 미포함 확정**(취소 버튼 디자인 미사용).
- 2026-06-02: **D2-b 확정 — `category` 추가** (사용자 결정 "카테고리 추가, 다른 곳도 추가한 듯"). favorite-stores 응답 7필드로 확정, BE 에 `Store.category` 응답 포함 요청. `reservation-list.category`(z.string()) 와 동일 컨벤션.
- 2026-06-02: **D1 확정 — 즉시 삭제·교체(A)** (사용자 선택). 사용처 전수 조사 결과 기존 `like` 소비자는 favorites page 1곳(교체 대상)뿐, 타 화면 0건 → deprecate 불필요. `like.ts`/`toggle-like`/`setLike`/MSW `/like` 전부 `favorite` 로 즉시 교체. **남은 미결: D4(404/409 BE), D5(empty 디자인), D7(limit) — 모두 FE 착수 비차단.**

## Outcome

- Status: **planned — 착수 가능**. 확정: D1(즉시 삭제·교체) / D2(옵션 B) / D2-b(category 추가, 7필드) / D3(낙관적 제거+완료 토스트, 언두 없음). 남은 미결은 모두 **FE 착수 비차단**: D4(404·409 에러스펙, BE 대기 — 임시 분기) / D5(empty 디자인 — 임시 placeholder) / D7(limit 10 vs 20, 사소). → contract/mock 확정, FE 카드/목록/토글 착수 가능.
- Follow-up: 결정 수령 → ① shared `favorite.ts` 신설 + like 교체 ② mock favorite 핸들러/seed ③ FE 무한스크롤 + 낙관적 토글(디자인 확보 후) ④ BE favorite use-case.
