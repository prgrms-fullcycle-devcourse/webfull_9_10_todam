# Feature Plan: 유저 예약 - 리뷰 작성, 나의 리뷰 수정 (user / review)

## Summary

- Goal: 인증된 User 가 본인 예약(체험 완료, `IN_PROGRESS` 이상)에 대해 **리뷰를 신규 작성**하고, 본인이 작성한 리뷰를 **수정**한다. 별점(필수) + 본문(선택, ≤500자) + 사진(선택, ≤3장·1장 5MB) 입력. 사진은 presigned → S3 PUT → 최종 작성/수정 요청 바디에 S3 Key 목록(`photos: string[]`) 일괄 제출. 작성 완료/수정 완료 시 예약 상세 + 리뷰 데이터 invalidate. 2 기능을 review 도메인 user side **write** 트랙으로 묶음.
- Owner: (BE 미정 / FE 미정)
- Date: 2026-06-02

## Status

<!--
게이트가 읽는 체크리스트. 셋 다 [x] 여야 completed/ 이동 가능 (pre-commit이 강제).
- API 구현: 실 BE(`apps/api`) 엔드포인트가 contract대로 존재·동작. MSW mock만 있으면 미체크.
- UI 구현: 화면/컴포넌트 구현 완료.
- API 연동: **실 API** 요청/응답이 contract 스키마로 연결.
-->

- [ ] API 구현
- [ ] UI 구현
- [ ] API 연동

## Context

<!-- 정본은 메인이 붙여넣은 Notion DB 발췌. -->

- 요구사항명세서(고정): `docs/requirements.md` (`리뷰 review` §2 리뷰 작성 — 작성 가능 조건/필수 rating/선택 content·photos·500자·3장·5MB / §3 리뷰 수정 — 30일·수정 가능 필드 rating·content·photos / `예약 reservation` §7 체험 완료 처리 — `IN_PROGRESS` 전이로 작성 가능). `artwork` §6 사진 업로드(JPG/PNG/HEIC·5MB·BullMQ 썸네일 240x240) — 리뷰 사진 업로드 정책과 동일 패턴.
- 기능명세 1: "리뷰 작성" (Notion 기능명세 DB `b242ee66b06c8349805601ce4a05247a` — 메인 발췌) — user/review, 우선순위 상, 연관화면 "예약 상세". 선행조건/트리거/동작/예외 정상 입력. 비고: **"리뷰 작성 완료 후 리뷰 수정 가능 여부 정책 정의 필요"** (D17).
- 기능명세 2: "나의 리뷰 수정" (동일 DB — 메인 발췌) — user/review, 우선순위 하, 연관화면 "나의 리뷰 상세". 선행조건/트리거/동작/예외 정상 입력. 비고: **"리뷰 수정 이력 저장 여부 정책 정의 필요"** (D18).
- API명세 1 (작성): `POST /reservations/{reservationId}/review` (Notion API명세 DB `5852ee66b06c838bb8ec01c6bf4f2e25` — 메인 발췌). 정본 그대로 §API Contract A 에 박음. body `{ rating, content, photos: string[](S3 Key) }`. 응답 201 `data.review = { id, reservationId, rating, content, photos: [{id, imageUrl}], createdAt }`.
- API명세 2 (수정): `PATCH /reviews/{reviewId}` (동일 DB — 메인 발췌). 정본 그대로 §API Contract B 에 박음. body `{ rating, content, photos: string[](S3 Key) }`. 응답 200 `data.review = { id, rating, content, photos: string[](URL), updatedAt }` — **POST/GET 응답과 shape 불일치(D15).** Content-Type multipart 표기 vs JSON body 예시 불일치(D16).
- API명세 3 (사진 업로드 presigned): `POST /review/images/presigned` (동일 DB — 메인 발췌). **명세 본문 전체 빈 값**(요구사항/Body/시스템처리/응답 모두 미입력). 메서드 POST + URI 만 정의. **큰 drift — D14 Open decision.** 기존 store/program presigned 패턴(SSOT 후보) detect 완료.
- 선행 plan(SSOT 재사용 — 매핑/토큰 신설 금지):
  - `docs/exec-plans/active/유저 예약 - 나의 리뷰 상세 조회, 나의 리뷰 삭제.md` (자매 plan, PR #104) — **리뷰 상세 라우트 `/my/reservations/[id]/review`(page.tsx + `_components/ReviewDetailClient/ReviewDetailContent/ReviewMoreMenu/DeleteReviewDialog`) 이미 dev 머지 완료**. shared `review-actions.ts`(삭제) + `features/review/actions/{api,queries,index}.ts`(deleteReview/useDeleteReviewMutation) 존재. `reviewDetailSchema`/`reviewDetailResultSchema`/`ReviewDetailResult`(`packages/shared/src/contracts/reservation-detail.ts` L89~108) 정의됨. 본 plan은 **수정 화면 prefill 용으로 GET 재사용**, 작성/수정 화면은 **별도 라우트 신설**(상세 라우트와 충돌 회피 — D19).
  - `docs/exec-plans/active/유저 예약 - 예약 상세조회.md` — 진입점 `ReviewSection`(`apps/web/src/app/(user)/my/reservations/[id]/_components/ReviewSection.tsx`) 의 빈 상태 "리뷰 쓰기" 버튼(L24~30 toast placeholder). `useReservationReview` hook.
  - `docs/exec-plans/active/user-예약-나의 예약조회.md` — `ReservationStatus` 매핑, `formatScheduled`.
  - `apps/web/src/features/program/edit/{api,queries}.ts` + `packages/shared/src/contracts/program-edit.ts` — **presigned 정본 부재(D14) 시 SSOT 후보**: request `{ fileName, fileType, isThumbnail }`, response `{ programImageId, uploadUrl, imageUrl }`, `uploadToS3(uploadUrl, file)`(PUT, Authorization 제외). `useUploadProgramImage` 이 presigned→S3 PUT 순차 처리 패턴.
- 접근주체/가드: `User` 이상(`@UseGuards(AuthGuard)`). 작성: 예약 본인 검증(`Reservation.userId === currentUserId`) + `IN_PROGRESS` 이상 + 180일 이내 + 기존 리뷰 없음. 수정: 리뷰 본인 검증(`Review.userId === currentUserId`) + 30일 이내. 미일치 시 `403 FORBIDDEN`.

### Drift 점검 (Notion 정본 ↔ 본 plan 채택)

**작성 (기능명세 1 / API명세 1)** — 본문·API 정본 일치. POST 응답 shape 명확(`photos: [{id, imageUrl}]` + `createdAt`). 비고에 "수정 가능 여부 정책"(D17) 미정.

**수정 (기능명세 2 / API명세 2)** — 본문 일치. 단 ① 응답 shape 가 POST/GET 와 불일치(`photos` URL 문자열배열 / `updatedAt` / `reservationId` 없음 — D15), ② Content-Type multipart 표기 vs JSON body 예시 불일치(D16), ③ 401/404 에러 미명시(보정), ④ 비고 "수정 이력 저장 여부"(D18) 미정.

**presigned (API명세 3)** — **본문 전체 빈 값.** 메서드+URI 만 존재. 가장 큰 drift(D14). store/program presigned 패턴 SSOT 격상 여부 BE/메인 결정 필요.

### 기능명세/API명세 ↔ 본 plan drift (사람 결정 필요)

| # | 항목 | 정본 상태 | 본 plan 채택 / 처리 |
|---|---|---|---|
| D14 | presigned 명세 본문 전체 부재 | `POST /review/images/presigned` — 요구사항/Body/시스템처리/응답 모두 빈 값. 메서드+URI 만 정의 | **잠정: store/program presigned 패턴 SSOT 격상** — request `{ fileName, fileType }`(리뷰는 isThumbnail 없음), response `{ uploadUrl, key }` 또는 `{ uploadUrl, imageUrl }`. 단 POST/PATCH review 가 받는 건 **S3 Key 문자열**(`photos: string[]`)인데 program presigned 응답은 `imageUrl`(원본 URL) — **key vs url 단위 불일치**. BE 가 presigned 응답에 S3 `key` 를 반드시 포함해야 photos 바인딩 가능. **BE 명세 확정 필요.** |
| D15 | PATCH 응답 shape 불일치 | POST/GET: `photos: [{id, imageUrl}]` + `createdAt` + `reservationId`. PATCH: `photos: string[](URL)` + `updatedAt` + `reservationId` 없음 | **FE prefill/갱신 정규화** — 수정 화면 prefill 은 GET(`{id, imageUrl}`) 사용. PATCH 응답은 invalidate 후 재조회(GET)로 갱신하므로 PATCH 응답 photos 형태에 FE 직접 의존 안 함(invalidate 우선). **BE 에 POST/GET 와 통일 권장** 기록. PATCH 응답 zod 는 별도 schema(`reviewUpdateResponseSchema`)로 정본 그대로 수용. |
| D16 | PATCH Content-Type 불일치 | 헤더 `multipart/form-data` 표기 / Body 예시는 JSON | **S3 Key 방식(presigned 선행 업로드)이면 `application/json` 이 자연스러움.** 본 plan 은 작성(POST)과 동일하게 `application/json` 채택. multipart 는 파일을 API 가 직접 받는 방식이라 presigned 흐름과 모순. **BE 확인 필요** — multipart 확정 시 FE 업로더 전면 변경. |
| D17 | "리뷰 작성 후 수정 가능 여부 정책" | 기능명세 1 비고 미정 | API명세 2(`PATCH /reviews/{reviewId}`) 가 존재하고 요구사항 §3 이 30일 이내 수정 허용 명시 → **수정 가능 채택**. 본 plan 이 작성+수정 동시 구현으로 정책 확정. |
| D18 | "리뷰 수정 이력 저장 여부" | 기능명세 2 비고 미정 | MVP 외 — `Review.updatedAt` 갱신만(요구사항 §3 처리 "필드 갱신, updatedAt 기록"). 별도 이력 테이블 미신설. **BE 결정.** |
| D19 | 작성/수정 라우트 충돌 | 자매 plan(PR #104)이 `/my/reservations/[id]/review` 를 **리뷰 상세 화면**으로 확정·구현 | **별도 라우트 신설 확정** — 작성 `/my/reservations/[id]/review/write`, 수정 `/my/reservations/[id]/review/edit`. (대안 `/new`·`/[reviewId]/edit` 검토했으나 reservationId 1:1 이라 단순 경로 채택.) **디자인 확보 시 최종 확정.** |
| D20 | 별점 interactive 입력 컴포넌트 부재 | `@todam/ui` `Rating`(`packages/ui/src/components/Rating.tsx`)은 `scope` prop 읽기전용 표시(StarFill 36×36 색만 분기). 클릭 입력 없음. interactive 컴포넌트 grep 결과 부재 | **interactive 별점 입력 컴포넌트 신설 필요** — `RatingInput`(`@todam/ui` 또는 feature local). 별 클릭 → onChange(1~5), hover 프리뷰. 디자인 토큰(별 크기/색/gap) Figma 확보 후 확정. UI: DESIGN.md 준수. |
| D21 | `MAX_REVIEW_PHOTO_COUNT` 상수 부재 | `packages/shared/src/constants/file.ts` — `MAX_FILE_SIZE_BYTES=5MB`, `ALLOWED_IMAGE_TYPES`, `MAX_ARTWORK_PHOTO_COUNT=5` 존재. 리뷰 3장 상수 없음 | **`MAX_REVIEW_PHOTO_COUNT = 3` 신설** (`file.ts`). 요구사항 §2/기능명세 "최대 3장". `MAX_FILE_SIZE_BYTES`·`ALLOWED_IMAGE_TYPES` 재사용. |
| D22 | 작성 deadline 180일 vs 수정 deadline 30일 기준 시점 혼재 | 작성: API명세 "IN_PROGRESS 도달 시점부터 180일" / 요구사항 §2 동일. 수정: API명세 "IN_PROGRESS 도달 시점부터 30일" + 요구사항 §3 "IN_PROGRESS 전이 후 30일" / 단 API명세 수정 에러 메시지·시스템처리엔 "작성일로부터 30일"으로 표기 — **기준 시점 혼재** | **작성 가드**: 진입 시 180일 초과면 작성 비활성(BE 가 `REVIEW_DEADLINE_EXCEEDED` 400 별건 검증). **수정 가드**: D13(자매 plan) 이미 `review.createdAt + 30일` 으로 MoreMenu "수정하기" 비활성 구현 — 기준이 "작성일". 요구사항은 "IN_PROGRESS 전이 후 30일". **BE 가 기준 시점 통일·확정 필요.** FE 는 BE 응답 기준 필드(createdAt 등)에 맞춰 가드. |
| D23 | PATCH 401/404 에러 미명시 | API명세 2 에러 정본에 400/403/500 만. 401·404 누락 | 공통 `AuthGuard` 기준 `401 UNAUTHORIZED` + reviewId 없을 시 `404 REVIEW_NOT_FOUND` 보정. 자매 plan `ReviewActionsErrorCode` 와 정합. |
| D24 | 작성 중 화면 이탈 확인 모달 | 기능명세 1 동작 "작성 중 화면 이탈 시 작성 중단 확인 모달 노출" | **FE 처리** — dirty 상태에서 라우트 이탈/뒤로가기/`beforeunload` 시 confirm 모달. 선행 `Modal`/dialog 패턴 재사용. 디자인 토큰 확보(Figma `8507:22751`) — §Design tokens 반영 완료. |
| D25 | **FSD 레이어 배치 규칙** (팀 신규 합의) | 이은지 Slack + PR #92 코멘트: `app/`=라우팅 페이지만 / `entities/`=데이터 출력(조회·표시용 카드·아이템, 여러 화면 재사용) / `features/`=유저 인터랙션·서버 통신(유즈케이스 컴포넌트) | **본 plan 컴포넌트 배치 재정의**: 작성/수정 폼은 **유저 인터랙션+서버 통신** → `features/review/` 배치(현 `app/.../review/_components/` 직배치 지양). `RatingInput`(입력 위젯)은 `@todam/ui` 공용. 리뷰 표시 카드(작품 헤더 등)는 `entities/` 후보. **자매 plan #104(상세/삭제)·아트워크 #92 의 기존 `_components` 배치도 동일 규칙으로 재정렬 대상 — 스코프는 사용자 확인 후.** |

### Open decisions

**BE 명세 확정 대기 (가장 시급)**: D14(presigned 응답에 S3 key 포함 — 미정 시 작성/수정 사진 흐름 전체 블록) / D16(PATCH Content-Type multipart vs json) / D15(PATCH 응답 shape 통일) / D22(deadline 기준 시점 통일).
**정책 확정(본 plan 이 해소)**: D17(수정 가능 — 채택).
**BE 결정**: D18(수정 이력 — MVP 외 잠정).
**FE 처리**: D19(라우트 분리) / D20(RatingInput 신설) / D21(MAX_REVIEW_PHOTO_COUNT) / D23(401/404 보정) / D24(이탈 확인 모달) / **D25(FSD 레이어 배치 — features/entities)**.
**Figma 확보 완료**: 작성/수정 3프레임(`8507:22575`/`22661`/`22751`) 수신 → §Design tokens 채움. RatingInput·이미지 업로더·이탈 모달 토큰 확정.
**사용자 확인 필요**: D25 FSD 재정렬 스코프 — 본 plan(신규)만 features/entities 규칙 적용할지, 자매 plan #104·아트워크 #92 기존 코드까지 소급 재정렬할지.

- **D14 채택 근거**: `apps/web/src/features/program/edit/api.ts` L31~64 + `queries.ts` L36~50 가 presigned→S3 PUT 순차 패턴. mock `handlers.ts` L93~109(store presigned). 단 program 응답은 `imageUrl`(URL)인데 review POST/PATCH 는 `photos: string[](S3 Key)` 수용 → **presigned 응답에 `key` 필드 필수.** BE 확정 전 contract 미확정.
- **D19 채택 근거**: 자매 plan PR #104 가 `/my/reservations/[id]/review/page.tsx` 를 상세 화면으로 점유. 본 plan write/edit 는 하위 경로 분리.
- **D20 채택 근거**: `Rating.tsx` 는 `role="img"` 읽기전용. 입력용 별점 컴포넌트 코드베이스 부재(grep 확인). 신설.

## API Contract (스냅샷)

<!-- 정본(Notion API명세) 기준 고정. BE/FE/reviewer 가 바인딩하는 SSOT.
     작성(POST)/수정(PATCH) 정본 그대로 박음. presigned(POST images)는 정본 부재 → D14 로 표시. -->

### 데이터모델 (`apps/api/prisma/schema.prisma` 스냅샷)

**`Review`** (model `reviews`) — 본 plan write 대상.

| 필드 | 타입 | 비고 |
|---|---|---|
| `id` | `String @id @default(uuid()) @db.Uuid` | PATCH path param `reviewId`. POST 응답 `review.id` |
| `reservationId` | `String @unique @map("reservation_id") @db.Uuid` | 예약↔리뷰 **1:1**(unique). POST path param `reservationId`. 예약당 1건 제한(409 REVIEW_ALREADY_EXISTS) 근거 |
| `userId` | `String @map("user_id") @db.Uuid` | 가드 — 작성: `Reservation.userId` / 수정: `Review.userId === currentUserId` |
| `storeId` | `String @map("store_id") @db.Uuid` | 통계 재계산 대상(평균 별점·리뷰 수) |
| `rating` | `Int @db.SmallInt` | 1~5. **필수**. request `rating` |
| `content` | `String?` | 최대 500자(요구사항 §2). **선택**. request `content` |
| `isVisible` | `Boolean @default(true) @map("is_visible")` | 신고/미노출 — 본 plan 범위 외 |
| `createdAt` | `DateTime @default(now()) @map("created_at") @db.Timestamptz(6)` | POST 응답 `createdAt`. D13/D22 수정 가드 기준(잠정) |
| `updatedAt` | `DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)` | PATCH 응답 `updatedAt` |
| (relations) | `photos ReviewPhoto[]`, `reservation Reservation`, `store Store`, `user User` | photos 응답 포함 |

**`ReviewPhoto`** (model `review_photos`) — 리뷰 사진. 작성/수정 시 S3 Key 배열로 일괄 생성, 수정 시 cascade 파기 후 재생성.

| 필드 | 타입 | 비고 |
|---|---|---|
| `id` | `String @id @default(uuid()) @db.Uuid` | POST 응답 `photos[].id` |
| `reviewId` | `String @map("review_id") @db.Uuid` | FK → `Review` |
| `imageUrl` | `String @db.VarChar(1000) @map("image_url")` | S3 Key 로부터 BE 가 구성. 응답 `photos[].imageUrl` |
| `thumbnailUrl` | `String? @db.VarChar(1000) @map("thumbnail_url")` | 240x240 (요구사항 §2). BullMQ 비동기 생성 |
| `sortOrder` | `Int @default(0) @map("sort_order")` | photos 배열 순서대로 부여 |
| `createdAt` | `DateTime @default(now()) @map("created_at") @db.Timestamptz(6)` | 응답 미노출 |

### 엔드포인트 A — 리뷰 작성

`POST /reservations/{reservationId}/review` — 인증 사용자 본인 예약의 리뷰 신규 작성. (API명세 1 정본 그대로.)

**Headers:** `Content-Type: application/json`, `Accept: application/json`, `Authorization: Bearer {accessToken}`

**Path Parameters:**
- `reservationId: string(uuid)`

**Request Body (정본 그대로):**
```json
{
  "rating": 5,
  "content": "정말 즐거운 체험이었어요.",
  "photos": ["reviews/photos/uuid-..._review_photo.jpg"]
}
```

| 필드 | 타입 | 필수 | 비고 |
|---|---|---|---|
| `rating` | `number(1~5)` | ✅ | 별점 미입력 시 등록 불가 |
| `content` | `string(≤500)` | 선택 | 요구사항 §2 최대 500자 |
| `photos` | `string[](≤3)` | 선택 | **S3 Key 목록**(presigned 선행 업로드 완료분). 1장당 5MB·JPG/PNG/HEIC |

**시스템 처리 (정본 그대로):**
1. 토큰 식별(AuthGuard).
2. 예약 소유자 검증 — 미일치 `403 FORBIDDEN`. 비존재 `404 RESERVATION_NOT_FOUND`.
3. 상태 `IN_PROGRESS` 이상 검증.
4. `IN_PROGRESS` 도달 시점부터 180일 이내 + 기존 리뷰 없음 — 초과 `400 REVIEW_DEADLINE_EXCEEDED`, 중복 `409 REVIEW_ALREADY_EXISTS`.
5. `reviews` 레코드 생성(rating, content).
6. `photos` S3 Key 배열 순회 → `review_photos` 일괄 삽입(sortOrder).
7. BullMQ 비동기 썸네일·최적화.
8. 공방·프로그램 평균 별점·리뷰 수 실시간 갱신.
9. 응답.

**응답 201 Created — 공통 봉투 (정본 그대로):**
```json
{
  "statusCode": 201,
  "timestamp": "2026-06-02T10:00:00.000Z",
  "path": "/reservations/res-uuid-001/review",
  "message": "리뷰가 성공적으로 등록되었습니다.",
  "data": {
    "review": {
      "id": "review-uuid-001",
      "reservationId": "res-uuid-001",
      "rating": 5,
      "content": "정말 즐거운 체험이었어요.",
      "photos": [
        { "id": "photo-uuid-001", "imageUrl": "https://cdn.todam.app/reviews/.../001.jpg" }
      ],
      "createdAt": "2026-06-02T10:00:00.000Z"
    }
  },
  "error": null
}
```

**data.review 스키마 — `reviewDetailSchema`(`reservation-detail.ts` L89~108) 재사용 가능** (POST 응답 = GET 응답과 동일 shape).

| 필드 | 타입 | 출처 |
|---|---|---|
| `id` | `string(uuid)` | `Review.id` |
| `reservationId` | `string(uuid)` | `Review.reservationId` |
| `rating` | `number(1~5)` | `Review.rating` |
| `content` | `string` | `Review.content` (null → 빈 문자열) |
| `photos` | `Array<{ id: string; imageUrl: string }>` | `ReviewPhoto[]` sortOrder ASC |
| `createdAt` | `string(ISO8601)` | `Review.createdAt` |

**에러 (정본 그대로):**

| status | code | message |
|---|---|---|
| 400 | `REVIEW_DEADLINE_EXCEEDED` | "리뷰 작성 기한(체험일로부터 180일 이내)이 이미 초과되어 작성할 수 없습니다." |
| 401 | `UNAUTHORIZED` | "인증 정보가 유효하지 않거나 만료되었습니다." |
| 403 | `FORBIDDEN` | "본인이 참여한 예약 정보에 대해서만 리뷰 작성이 허용됩니다." |
| 404 | `RESERVATION_NOT_FOUND` | "요청하신 예약 정보를 찾을 수 없습니다." |
| 409 | `REVIEW_ALREADY_EXISTS` | "이미 리뷰를 작성한 예약입니다." |
| 500 | `INTERNAL_SERVER_ERROR` | "리뷰 등록 중 서버 오류가 발생했습니다." |

### 엔드포인트 B — 리뷰 수정

`PATCH /reviews/{reviewId}` — 인증 사용자 본인 리뷰 수정. (API명세 2 정본 그대로 + D15/D16/D23 보정.)

**Headers:** `Content-Type: application/json`(D16 — 정본 multipart 표기이나 S3 Key 방식이므로 json 채택, BE 확인), `Accept: application/json`, `Authorization: Bearer {accessToken}`

**Path Parameters:**
- `reviewId: string(uuid)`

**Request Body (정본 그대로 — POST 와 동일):**
```json
{
  "rating": 4,
  "content": "수정된 본문입니다.",
  "photos": ["reviews/photos/uuid-..._edited.jpg"]
}
```

| 필드 | 타입 | 필수 | 비고 |
|---|---|---|---|
| `rating` | `number(1~5)` | ✅ | 미입력 시 저장 불가 |
| `content` | `string(≤500)` | 선택 | |
| `photos` | `string[](≤3)` | 선택 | **최종 확정 S3 Key 목록 전체**. 기존 review_photos cascade 파기 후 통째 재생성 |

**시스템 처리 (정본 그대로):**
1. 토큰 식별(AuthGuard) — 미인증 `401 UNAUTHORIZED`(D23 보정).
2. reviewId 조회 + 작성자 소유 검증 — 미일치 `403 FORBIDDEN`. 비존재 `404 REVIEW_NOT_FOUND`(D23 보정).
3. 수정 허용기한(30일) 체크 — 초과 `400 REVIEW_EDIT_DEADLINE_EXCEEDED`. (기준 시점 D22 — BE 확정.)
4. `reviews` rating·content 업데이트.
5. `photos` 있으면 기존 `review_photos` 제거 후 신규 S3 Key 로 재생성.
6. BullMQ 비동기 썸네일.
7. 공방·프로그램 평균 별점 재계산.
8. 응답.

**응답 200 OK — 공통 봉투 (정본 그대로 — POST/GET 와 shape 불일치 D15):**
```json
{
  "statusCode": 200,
  "timestamp": "2026-06-02T10:00:00.000Z",
  "path": "/reviews/review-uuid-001",
  "message": "리뷰가 성공적으로 수정되었습니다.",
  "data": {
    "review": {
      "id": "review-uuid-001",
      "rating": 4,
      "content": "수정된 본문입니다.",
      "photos": ["https://cdn.todam.app/reviews/.../001.jpg"],
      "updatedAt": "2026-06-02T11:00:00.000Z"
    }
  },
  "error": null
}
```

**data.review 스키마 — 별도 `reviewUpdateResponseSchema` 신설 (D15 — POST/GET 와 불일치):**

| 필드 | 타입 | 출처 | 비고 |
|---|---|---|---|
| `id` | `string(uuid)` | `Review.id` | |
| `rating` | `number(1~5)` | `Review.rating` | |
| `content` | `string` | `Review.content` | |
| `photos` | `string[]` | `ReviewPhoto[].imageUrl` | **URL 문자열 배열**(POST 는 `{id,imageUrl}` 객체배열). 통일 권장(D15) |
| `updatedAt` | `string(ISO8601)` | `Review.updatedAt` | POST 는 `createdAt`. `reservationId` 없음 |

> **D15 처리**: FE 는 PATCH 응답 photos 형태에 직접 의존하지 않고, 성공 후 GET 재조회(invalidate)로 화면 갱신. PATCH 응답 zod 는 정본 그대로 수용하되 BE 에 POST/GET 통일 권장.

**에러 (정본 + D23 보정):**

| status | code | message | 비고 |
|---|---|---|---|
| 400 | `REVIEW_EDIT_DEADLINE_EXCEEDED` | "리뷰 수정이 가능한 기한(작성일로부터 30일 이내)이 경과하여 수정을 완료할 수 없습니다." | 정본 그대로. 기준 시점 D22 |
| 401 | `UNAUTHORIZED` | "인증 정보가 유효하지 않거나 만료되었습니다." | 정본 미명시 — D23 보정 |
| 403 | `FORBIDDEN` | "자신이 직접 등록한 리뷰에 대해서만 수정이 가능합니다." | 정본 그대로 |
| 404 | `REVIEW_NOT_FOUND` | "리뷰를 찾을 수 없습니다." | 정본 미명시 — D23 보정 |
| 500 | `INTERNAL_SERVER_ERROR` | "리뷰 수정 중 서버 오류가 발생했습니다." | 정본 그대로 |

### 엔드포인트 C — 리뷰 사진 업로드 (presigned) — **정본 부재 (D14)**

`POST /review/images/presigned` — 리뷰 사진 S3 presigned URL 발급. **Notion API명세 본문 전체 빈 값.** 아래는 store/program presigned 패턴 기반 **추론 contract (BE 확정 전 미고정)**.

**Headers:** `Content-Type: application/json`, `Accept: application/json`, `Authorization: Bearer {accessToken}`

**Request Body (추론 — program-edit 패턴):**
```json
{ "fileName": "review.jpg", "fileType": "image/jpeg" }
```

**응답 (추론 — D14 핵심 결정 포인트):**
```json
{
  "data": {
    "uploadUrl": "https://s3.../presigned-put-url",
    "key": "reviews/photos/uuid-..._review_photo.jpg"
  }
}
```

> **D14 — BE 확정 필수:** presigned 응답에 **S3 `key`** 가 반드시 포함되어야 함. POST/PATCH review 의 `photos: string[]` 는 **S3 Key** 를 수용하는데, program presigned 응답은 `imageUrl`(URL)만 반환(`programImageUploadResultSchema`). 리뷰는 key 단위 → 응답 contract 가 다름. **BE 가 명세 본문 채울 때까지 contract 미확정.** FE 업로드 흐름: ① `POST /review/images/presigned` → ② `uploadToS3(uploadUrl, file)`(PUT, Authorization 제외 — `program/edit/api.ts` L55~64 재사용) → ③ 받은 `key` 들을 POST/PATCH `photos[]` 에 담아 제출.

**에러 (추론 — BE 확정):** 401 / 400 INVALID_FILE_TYPE / 413 파일 초과 / 500.

## Design tokens

**Figma 정본 수신 완료** (2026-06-02). 작성/수정 화면 3프레임 — `8507:22575`(작성 빈 상태) / `8507:22661`(입력+사진 채움) / `8507:22751`(이탈 확인 다이얼로그). 아래 토큰으로 §UI 착수 가능(presigned D14·deadline D22 BE 확정은 사진/가드 로직 한정 블록, 화면 레이아웃은 선행 가능).

### Screen (작성/수정 공통)

- 페이지 bg `#FBF8F3`, 컨테이너 360 width.
- **Header type=popup** — 좌측 close-X 56×56 tap target(아이콘 `CloseIcon`, `#191E25`). title 중앙 없음(작성은 헤더 타이틀 대신 본문 상단 질문 카피로 처리). 뒤로가기 대신 닫기(X) → 이탈 확인(D24) 트리거.
- 본문 상단 타이틀 카피 "이번 체험은 어떠셨나요?" (Bold, `#191E25`).
- 콘텐츠 좌우 패딩 16, 섹션 간 vertical gap.

### RatingInput (D20 신설) — interactive

- 별 36×36, 5개 가로 배열, gap.
- 채움 `#1D5628`(primary-darker, `StarFillIcon`) / 빈 `#7D838D`(foreground-tertiary, `StarIcon`).
- 클릭 → onChange(1~5). hover/press 프리뷰. 각 별 36×36 hit target.

### TextArea (본문)

- label "어떤 점이 좋았나요? (선택)".
- input 328×128 radius 12, bg `#FFFFFF`, border `#E2E4E7`(border-subtle).
- helperText `0/500자` **visible:true** (상세 plan 은 false). 우하단 카운터.
- 본문 텍스트 Medium 16/20 `#191E25`.

### 이미지 업로더 (최대 3장, D21)

- **add 슬롯**: `DescriptionBlock` 112×80 radius 16, dashed border `#C8CBD1`(border-default), 중앙 `CameraIcon` `#7D838D`. 탭 → 파일 선택.
- **채움 슬롯**: 112×80 radius 16, `<img>` cover. 우상단 삭제 버튼 — 20×20 원형 `#434A54`(foreground-secondary) + `CloseIcon` 흰색.
- 슬롯 가로 배열(add + 채움 썸네일). 3장 도달 시 add 슬롯 숨김. 업로드 중 placeholder(스피너/딤).

### ButtonSection (등록/수정 완료) — sticky bottom

- sticky 컨테이너 360×100, 상단 구분.
- 버튼 328×56 radius 16, bg `#1D5628`(primary-darker), 라벨 흰색 — 작성 "리뷰 등록" / 수정 "수정 완료".
- rating 미입력·pending 시 disabled.

### 이탈 확인 다이얼로그 (D24) — `8507:22751`

- **Modal type=shortText** 320×188 radius 24, overlay `#0E1218@80%`(dimmed).
- title "리뷰 작성을 중단할까요?" / description "지금 나가면 쓴 내용이 모두 사라져요."
- 버튼 2개: **계속하기** bg `#F1F2F4`(muted, secondary) / **나가기** bg `#1D5628`(primary-darker, 확정 액션).
- 자매 plan `DeleteReviewDialog`(Modal type=shortText) 패턴 재사용 — 단 danger 색 아님(나가기=primary). 수정 화면 이탈 카피는 "리뷰 수정을 중단할까요?"로 분기.

### 선행 plan 재사용 토큰 (변경 금지)

- `Rating` 컴포넌트(`@todam/ui`) — **표시 전용**. 입력은 D20 신설.
- TextArea(`@todam/ui` `TextArea`) — `0/500자` helperText 패턴(자매 plan §TextArea: input 328×128 radius 12 bg `#FFFFFF` border `#E2E4E7`, text Medium 16/20 `#191E25`).
- Photo grid 토큰(자매 plan): 각 photo 112×80 radius 16 bg `#F1F2F4`(muted).
- `Modal`(`@todam/ui`) + 자매 `DeleteReviewDialog`(overlay `#0E1218@80%`, modal radius 24) — 이탈 확인 모달 베이스.
- `formatScheduled`, `ReservationDetail`(작품 정보 헤더 "머그컵 만들기" + "흙과 사람・5월 14일 (목)").

## UI 동작 흐름 (기능명세 + 정본 기반)

### A. 리뷰 작성 흐름 (기능명세 1)

1. **진입점**: 예약 상세 `ReviewSection`(`_components/ReviewSection.tsx` L24~30) — `hasReview === false` + `IN_PROGRESS` 이상 일 때 빈 상태 "리뷰 쓰기" 버튼. 현재 `handleWriteReview` 가 toast placeholder → **본 plan 이 `/my/reservations/[id]/review/write` 라우팅으로 교체(D19).** 180일 초과 시 버튼 비활성/숨김(D22 — BE 응답 기준).
2. **화면 렌더**: 작품 정보 헤더(클래스명 + 공방·체험일) + RatingInput(필수, D20) + TextArea(본문, 0/500자) + 이미지 업로더(최대 3장).
3. **이미지 등록**: 파일 선택 → 형식(JPG/PNG/HEIC)·용량(5MB)·개수(≤3) 클라이언트 검증 → presigned 발급(C, D14) → S3 PUT → key 적재. 미지원 형식/용량 초과/3장 초과 시 토스트 안내.
4. **등록 버튼**: rating 미입력 시 비활성. 클릭 → `POST /reservations/{id}/review` body `{ rating, content, photos: key[] }`. pending 중 버튼 비활성.
5. **성공 201**: 토스트 "리뷰가 등록되었어요." + invalidate `['reservations','detail',id]` + `['reservations','review',id]` → `router.back()`(예약 상세 복귀, `hasReview=true` 전이 → ReviewSection 작성됨 카드 노출).
6. **실패**: 400(기한 초과)·403·404·409(이미 작성)·500 → 각 토스트. 409 시 예약 상세 복귀.
7. **이탈(D24)**: dirty(별점/본문/사진 입력됨) 상태에서 back/라우트 이탈/`beforeunload` → "작성을 중단할까요?" confirm 모달.

### B. 리뷰 수정 흐름 (기능명세 2)

1. **진입점**: 리뷰 상세 화면 `ReviewDetailClient`(`review/_components/ReviewDetailClient.tsx` L52~54) MoreMenu "수정하기"(`ReviewMoreMenu` — D13 30일 가드 이미 구현). 현재 `handleEdit` 가 toast placeholder → **본 plan 이 `/my/reservations/[id]/review/edit` 라우팅으로 교체(D19).** 30일 초과 시 메뉴 비활성(기구현).
2. **prefill**: 진입 시 GET(`useReservationReview`, `reviewDetailSchema`) 로 기존 rating/content/photos 조회 → form 초기값. photos 는 `{id, imageUrl}` 객체배열(GET shape).
3. **수정**: RatingInput/TextArea/이미지 업로더 — 기존 사진 삭제 + 신규 추가 가능. 신규 사진은 presigned→S3 PUT, 유지 사진은 기존 key 보존. 최종 photos = 유지 key + 신규 key 통합 배열.
4. **수정 완료 버튼**: rating 미입력 시 비활성. 클릭 → `PATCH /reviews/{reviewId}` body `{ rating, content, photos: key[] }`(D16 json).
5. **성공 200**: 토스트 "리뷰가 수정되었어요." + invalidate `['reservations','detail',reservationId]` + `['reservations','review',reservationId]` → `router.back()`(리뷰 상세 복귀, GET 재조회로 최신 반영 — PATCH 응답 shape 의존 안 함, D15).
6. **실패**: 400(수정 기한 초과)·401·403·404·500 → 각 토스트.
7. **이탈(D24)**: 작성과 동일.

## Scope

- In:
  - BE:
    - `POST /reservations/{reservationId}/review` — AuthGuard, 예약 본인 가드, `IN_PROGRESS` 이상 + 180일 + 기존 리뷰 없음, `reviews` 생성 + `review_photos` S3 Key 일괄 삽입, BullMQ 썸네일 트리거, 통계 갱신, 공통 봉투, 400/401/403/404/409/500
    - `PATCH /reviews/{reviewId}` — AuthGuard, 리뷰 본인 가드, 30일 가드(D22), rating·content 갱신 + photos cascade 재생성, BullMQ·통계, 공통 봉투, 400/401/403/404/500
    - `POST /review/images/presigned` — AuthGuard, S3 presigned PUT URL + **key** 발급(D14 — 명세 본문 확정 필요)
  - shared:
    - `packages/shared/src/contracts/review-write.ts` 신설(또는 `review-actions.ts` 합류 — 결정):
      - `reviewWriteRequestSchema` (`{ rating: 1~5, content?: ≤500, photos?: string[]≤3 }`) — POST/PATCH 공용
      - POST 응답: `reviewDetailSchema` 재사용(`reservation-detail.ts`)
      - `reviewUpdateResponseSchema` 신설 (PATCH — `{ id, rating, content, photos: string[], updatedAt }`, D15)
      - presigned: `reviewImageUploadRequestSchema`(`{ fileName, fileType }`) + `reviewImageUploadResultSchema`(`{ uploadUrl, key }`) — **D14 확정 후 고정**
      - `ReviewWriteErrorCode` enum (`REVIEW_DEADLINE_EXCEEDED`/`REVIEW_EDIT_DEADLINE_EXCEEDED`/`REVIEW_ALREADY_EXISTS`/`RESERVATION_NOT_FOUND`/`REVIEW_NOT_FOUND`/`UNAUTHORIZED`/`FORBIDDEN`/`INVALID_FILE_TYPE`/`INTERNAL_SERVER_ERROR`)
      - `packages/shared/src/index.ts` 재노출
    - `packages/shared/src/constants/file.ts` — `MAX_REVIEW_PHOTO_COUNT = 3` 신설(D21)
  - UI(`@todam/ui`):
    - `RatingInput` 신설(D20) — interactive 별점 입력(`value`/`onChange(1~5)`, hover 프리뷰, 읽기전용 `Rating` 과 별개). Storybook 등록.
  - FE:
    - 라우트(D19):
      - `apps/web/src/app/(user)/my/reservations/[id]/review/write/page.tsx` — 서버 컴포넌트 params unwrap → 클라 위임
      - `apps/web/src/app/(user)/my/reservations/[id]/review/edit/page.tsx` — 동일
    - 컴포넌트 — **D25 FSD 배치**: 폼/업로더는 유저 인터랙션+서버 통신 → `features/review/write/components/`(또는 `ui/`). `app/.../review/write/page.tsx`·`edit/page.tsx` 는 라우팅+params unwrap 만 하고 feature 컴포넌트 위임. (현행 `_components` 직배치 지양):
      - `ReviewFormClient.tsx` — RatingInput + TextArea(0/500) + 이미지 업로더 + 등록/수정 버튼 + 이탈 모달(D24). write/edit 모드 prop 분기(prefill 여부). → `features/review/write/`
      - 이미지 업로더 컴포넌트 — presigned→S3 PUT 순차(`program/edit` 패턴), 썸네일 미리보기, 삭제, ≤3 가드. → `features/review/write/`
      - 리뷰 표시용(작품 헤더 카드 등 조회 전용·재사용)은 `entities/review/` 후보 — 단 본 plan 은 작성/수정 인터랙션 중심이라 신규 표시 카드 최소
    - features:
      - `apps/web/src/features/review/write/{api,queries,index}.ts` 신설 — `createReview(reservationId, body)` / `updateReview(reviewId, body)` / `uploadReviewImage(req)` + `uploadToS3` / `useCreateReviewMutation` / `useUpdateReviewMutation` / `useUploadReviewImage`. onSuccess: invalidate detail+review keys + toast + back
      - 또는 기존 `features/review/actions` 확장(작성/수정 합류 — 결정)
    - 진입점 교체:
      - `ReviewSection.tsx` L24~30 `handleWriteReview` toast → `router.push('/my/reservations/[id]/review/write')`
      - `ReviewDetailClient.tsx` L52~54 `handleEdit` toast → `router.push('/my/reservations/[id]/review/edit')`
  - MSW:
    - `apps/web/src/mocks/handlers.ts`:
      - `POST /api/v1/reservations/:reservationId/review` — 신설. 201 + `data.review`(reviewDetailSchema shape). `?simulate=400|403|404|409|500` 토글, `?unauth=1` → 401
      - `PATCH /api/v1/reviews/:reviewId` — 신설. 200 + `data.review`(update shape). 400/403/404/500 토글
      - `POST /api/v1/review/images/presigned` — 신설. `{ uploadUrl, key }` 반환(store presigned mock 패턴 — L93~109)
    - `apps/web/src/mocks/db.ts` — in-memory 리뷰 생성/갱신 시드(작성 후 GET 재조회 시 반영). `createReview`/`updateReview` 헬퍼 검토
  - 연동 검증: presigned→S3→key 흐름, rating 필수 가드, 180일/30일 가드, 작성 후 hasReview=true 전이, 수정 후 GET 재조회 최신화.

- Out:
  - 리뷰 상세 조회 / 삭제 (자매 plan PR #104 — 완료. 본 plan 은 GET 재사용·진입점만)
  - `DELETE /reviews/{reviewId}` (자매 plan §API Contract B)
  - 공방 퍼블릭 페이지 / 클래스 상세 리뷰 목록 (요구사항 §5 — 별도 plan)
  - 리뷰 신고 / 미노출 처리 (`Review.isVisible` — MVP 이후)
  - 평균 별점·리뷰 수 재계산 로직 자체 (BE 내부 — 본 plan 트리거만)
  - 리뷰 수정 이력 테이블 (D18 — MVP 외)
  - 파트너 측 리뷰 관리 (요구사항 §5 — MVP 이후)

## Plan

1. (선결정) Open decisions **D14·D16·D15·D22** 메인/BE 확정 수령:
   - D14: presigned 명세 본문 — 응답 `key` 포함 여부 (사진 흐름 전체 블록)
   - D16: PATCH Content-Type — multipart vs json (json 잠정)
   - D15: PATCH 응답 shape POST/GET 통일 여부
   - D22: 작성 180일 / 수정 30일 기준 시점 통일 (IN_PROGRESS 도달 vs 작성일)
2. (shared) `packages/shared/src/contracts/review-write.ts` 신설:
   - `reviewWriteRequestSchema`(POST/PATCH 공용) / POST 응답 `reviewDetailSchema` 재사용 / `reviewUpdateResponseSchema`(D15) / `reviewImageUploadRequestSchema`·`reviewImageUploadResultSchema`(D14 확정 후) / `ReviewWriteErrorCode`
   - `file.ts` `MAX_REVIEW_PHOTO_COUNT = 3`(D21)
   - `index.ts` 재노출
3. (UI) `@todam/ui` `RatingInput` 신설(D20) — interactive 별점. Storybook. (Figma 토큰 확보 후 시각 확정.)
4. (BE) `apps/api/src/modules/review` 확장(자매 plan 이 디렉터리 신설 — 그 위에 작성/수정/presigned use-case 추가):
   - `presentation/controllers/review.controller.ts` — `@Post(':reservationId/review')`(reservation 경로) + `@Patch('reviews/:reviewId')` + `@Post('review/images/presigned')` + AuthGuard + CurrentUser
   - `application/use-cases/create-review.use-case.ts` — 예약 본인 가드 + IN_PROGRESS + 180일 + 중복 검증 + photos S3 Key 삽입 + 통계
   - `application/use-cases/update-review.use-case.ts` — 리뷰 본인 가드 + 30일 + photos cascade 재생성 + 통계
   - `application/use-cases/issue-review-image-upload.use-case.ts` — presigned URL + key 발급(D14)
   - `infrastructure/storage/review-photo-storage.service.ts` — S3 presigned PUT(선행 작품/program storage 패턴)
   - 예외: REVIEW_DEADLINE_EXCEEDED/REVIEW_EDIT_DEADLINE_EXCEEDED/REVIEW_ALREADY_EXISTS 등 (공통 filter)
5. (FE) 라우트·컴포넌트 구현 (D14·디자인 확보 후 착수):
   - `review/write/page.tsx` + `review/edit/page.tsx`
   - `ReviewFormClient.tsx`(공용, write/edit 분기) + 이미지 업로더
   - 진입점 교체: `ReviewSection.tsx`(write) + `ReviewDetailClient.tsx`(edit)
6. (FE features) `features/review/write/{api,queries,index}.ts`:
   - `createReview`/`updateReview`/`uploadReviewImage`+`uploadToS3`
   - `useCreateReviewMutation`/`useUpdateReviewMutation`/`useUploadReviewImage` — onSuccess invalidate + toast + back
7. (mock) `handlers.ts` POST review / PATCH review / POST presigned 핸들러 + `db.ts` 시드 갱신
8. 연동 검증: presigned→S3→key, rating 가드, 180/30일 가드, 작성 후 hasReview 전이, 수정 후 재조회.

## Out (단계별 완료물)

- API: <!-- 구현된 엔드포인트, 파일 -->
- UI: <!-- 라우트/컴포넌트/contract/features/mock + typecheck·lint 결과 -->
- 연동: <!-- 연결 지점, 검증 결과 -->

## Risks

- **presigned 정본 완전 부재(D14).** 응답에 S3 `key` 미포함 시 POST/PATCH `photos[]` 바인딩 불가 → 사진 흐름 전체 블록. **최우선 BE 명세 확정.**
- **PATCH Content-Type 모순(D16).** multipart 확정 시 FE 업로더·body 직렬화 전면 재작성. S3 Key 방식과 모순 — BE 확인 전 json 가정 위험.
- **PATCH 응답 shape 불일치(D15).** FE 가 PATCH 응답에 직접 의존하면 photos URL/key·createdAt/updatedAt 정규화 버그. 본 plan 은 invalidate+GET 재조회로 회피하나 BE 통일이 근본 해결.
- **deadline 기준 시점 혼재(D22).** 작성 180일(IN_PROGRESS 도달) vs 수정 30일(API명세 "작성일" / 요구사항 "IN_PROGRESS 전이 후"). FE 가드(D13)는 `createdAt` 기준 — 요구사항과 어긋날 수 있음. BE 통일 + contract 에 기준 필드 명시 필요.
- **RatingInput 신설(D20).** 입력용 별점 컴포넌트 부재 → 신설. 접근성(키보드/aria)·hover 프리뷰·터치 hit target 고려. 디자인 토큰 의존.
- **FSD 레이어 배치(D25).** 팀 신규 합의로 폼/인터랙션은 `features/`, 표시 카드는 `entities/`, 페이지는 `app/`. 본 plan 신규 컴포넌트는 규칙 준수하되, 자매 #104·아트워크 #92 기존 `_components` 직배치 소급 재정렬은 이미 오픈된 PR 을 건드림 → 스코프·타이밍 사용자 확인 필요.
- **이탈 확인 모달(D24).** SPA route guard + `beforeunload` 조합 복잡. dirty 추적 누락 시 데이터 유실 또는 과도한 모달.
- **작성 중복(409)·예약당 1건.** ReviewSection 진입 가드 누락 시 작성 화면 재진입 → 409. hasReview·displayState 동기화 필수.
- **라우트 분리(D19) 확정 전 착수 시** 자매 plan 상세 라우트와 충돌. write/edit 경로 디자인 확정 필요.

## Validation

- Tests:
  - BE service 단위:
    - POST: 예약 본인 가드(403), 비존재(404), IN_PROGRESS 미만 거부, 180일 초과(400), 중복(409), photos key→review_photos 삽입·sortOrder, rating 1~5 경계
    - PATCH: 리뷰 본인 가드(403), 비존재(404), 30일 초과(400), photos cascade 재생성, rating 미입력 거부
    - presigned: key 발급·만료(D14)
  - BE controller: 201/200/400/401/403/404/409/500
  - shared zod: POST/PATCH 응답 fixture 파싱, request rating 범위·content 500자·photos 3장 거부, presigned 응답 파싱
  - FE:
    - `ReviewFormClient` write/edit 렌더 + rating 미입력 시 버튼 disabled + prefill(edit)
    - 이미지 업로더 — 형식/용량/개수 검증, presigned→S3 mock
    - mutation onSuccess invalidate + back
    - 이탈 모달 dirty 분기(D24)
    - `RatingInput` 클릭/hover/키보드(D20)
- Manual checks:
  - 빈 상태 "리뷰 쓰기" → write 화면 → 별점만 입력 후 등록 → 201 → 예약 상세 hasReview=true 카드
  - 사진 1/3/0장, 4MB/6MB(거부), gif(거부)
  - 본문 500자 경계, 501자 차단
  - 상세 MoreMenu "수정하기"(30일 이내) → edit prefill → rating 변경 + 사진 1장 교체 → 200 → 상세 최신화
  - 30일 초과 리뷰 → "수정하기" 비활성
  - 미작성 dirty 상태 back → 이탈 모달
  - 타인/비존재 직접 URL → 403/404
  - 180일 초과 예약 → 작성 진입 차단/400
- Observability:
  - 500 로깅(`review_create_failed`/`review_update_failed`/`review_presign_failed`)
  - 400(deadline)·409(duplicate) 분기 메트릭
  - S3 업로드 실패 / 썸네일 큐 실패 별도 로깅(BE)

## Decision Log

- 2026-06-02: 기능명세 "리뷰 작성"(우선순위 상) + "나의 리뷰 수정"(우선순위 하) 동시 plan. 작성은 본문/API 정본 명확. 수정은 비고 "수정 가능 여부 정책"(D17) → 본 plan 이 PATCH 존재 + 요구사항 §3 근거로 **수정 가능 채택**.
- 2026-06-02: `POST /reservations/{reservationId}/review`(API명세 1) 정본 그대로 §A 박음. body `{ rating, content, photos: S3 Key[] }`, 201 응답 `data.review`(GET 와 동일 shape — `reviewDetailSchema` 재사용 가능).
- 2026-06-02: `PATCH /reviews/{reviewId}`(API명세 2) 정본 그대로 §B 박음. **응답 shape POST/GET 와 불일치(D15)** — photos URL 문자열배열·updatedAt·reservationId 없음. FE 는 invalidate+GET 재조회로 갱신하여 PATCH 응답 직접 의존 회피. BE 통일 권장.
- 2026-06-02: PATCH Content-Type — 정본 `multipart/form-data` 표기이나 Body 예시 JSON + S3 Key 방식이므로 **`application/json` 잠정 채택(D16)**. BE 확인 필요(multipart 확정 시 FE 전면 변경).
- 2026-06-02: `POST /review/images/presigned`(API명세 3) **본문 전체 빈 값** — 가장 큰 drift(D14). store/program presigned 패턴(`program/edit/api.ts` + `programImageUploadResultSchema`) SSOT 후보 격상. 단 program 응답은 `imageUrl`(URL), review 는 `photos: string[](S3 Key)` 수용 → **presigned 응답에 `key` 필수.** BE 명세 본문 확정 전 contract 미고정.
- 2026-06-02: deadline 기준 시점 혼재(D22) — 작성 180일(IN_PROGRESS 도달, API명세+요구사항 일치), 수정 30일(API명세 에러문 "작성일" / 요구사항 §3 "IN_PROGRESS 전이 후"). 자매 plan D13 FE 가드는 `review.createdAt + 30일`. **BE 기준 통일 + contract 명시 필요.**
- 2026-06-02: 라우트 분리(D19) — 자매 plan PR #104 가 `/my/reservations/[id]/review`(page.tsx)를 **리뷰 상세 화면**으로 점유. 작성 `/review/write`, 수정 `/review/edit` 하위 경로 신설 확정. 디자인 확보 시 최종 확정.
- 2026-06-02: 진입점 — `ReviewSection.tsx` L24~30 `handleWriteReview`(toast) → write 라우팅, `ReviewDetailClient.tsx` L52~54 `handleEdit`(toast) → edit 라우팅. 둘 다 현재 placeholder. 본 plan 이 실제 라우팅 교체.
- 2026-06-02: 별점 입력 — `@todam/ui` `Rating`(`Rating.tsx`)은 `role="img"` 읽기전용 표시(grep 으로 interactive 부재 확인). **`RatingInput` 신설(D20)** — value/onChange/hover.
- 2026-06-02: `MAX_REVIEW_PHOTO_COUNT = 3` 신설(D21) — `file.ts`. `MAX_FILE_SIZE_BYTES`(5MB)·`ALLOWED_IMAGE_TYPES`(JPG/PNG/HEIC) 재사용.
- 2026-06-02: PATCH 401/404 미명시(D23) — 공통 AuthGuard 401 + reviewId 없을 시 404 REVIEW_NOT_FOUND 보정. 자매 `ReviewActionsErrorCode` 정합.
- 2026-06-02: 작성 중 화면 이탈 확인 모달(D24, 기능명세 1 동작) — FE dirty 추적 + route guard + beforeunload. 선행 `Modal`·자매 `DeleteReviewDialog` 패턴 베이스.
- 2026-06-02: 수정 이력 저장(D18, 기능명세 2 비고) — MVP 외. `Review.updatedAt` 갱신만(요구사항 §3). BE 결정.
- 2026-06-02: shared 위치 — `review-write.ts` 신설 vs `review-actions.ts`(자매 plan 삭제 contract) 합류 — request/응답 schema 분량 고려해 `review-write.ts` 신설 잠정. FE features 도 `features/review/write` 신설 vs `actions` 확장 — 신설 잠정. 구현 시 최종 결정.

## Outcome

- Status: planned (D14·D16·D15·D22 BE 명세 확정 대기 — 특히 D14 presigned 가 사진 흐름 전체 블록. D17 정책 해소(수정 가능). D19·D20·D21·D23·D24 FE 처리. **Figma 확보 완료 — §Design tokens 채움, 화면 레이아웃 착수 가능.** D25 FSD 배치 규칙 반영(features/entities) — 소급 재정렬 스코프만 사용자 확인 대기).
- Follow-up:
  - 메인/BE 가 D14 presigned 명세 본문(응답 key) 확정 → 사진 흐름 contract 고정
  - BE D16(Content-Type)·D15(PATCH 응답 통일)·D22(deadline 기준) 확정 → §API Contract 갱신
  - D25 FSD 소급 재정렬 스코프 사용자 확정 → 자매 #104·아트워크 #92 entities/features 재배치 여부
  - 별도 plan: 공방 퍼블릭 페이지 리뷰 목록(요구사항 §5), 리뷰 신고/미노출
