export enum UserRole {
  USER = "USER",
  PARTNER = "PARTNER",
  ADMIN = "ADMIN"
}

export enum ReservationStatus {
  REQUESTED = "REQUESTED",
  CONFIRMED = "CONFIRMED",
  CANCELED = "CANCELED",
  COMPLETED = "COMPLETED"
}

export enum ArtworkStatus {
  CREATED = "CREATED",
  IN_PROGRESS = "IN_PROGRESS",
  READY = "READY",
  DELIVERED = "DELIVERED"
}

